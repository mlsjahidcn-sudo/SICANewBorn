// Font preload optimization component
// Note: ReactDOM.preconnect should be used in a Client Component
// but preconnect links can also be added directly in the head

export function FontPreload() {
  return null;
}
