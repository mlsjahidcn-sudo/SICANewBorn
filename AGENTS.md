# AGENTS.md

## 项目概览
SICA (Study in China Academy) - 面向国际学生的中国高校及项目信息查询平台。作为教育机构SICA的官方平台，提供大学目录、大学详情、奖学金信息、招生要求等功能，并突出SICA的申请支持服务。

**目标用户**：计划赴中国留学的国际学生（中国大陆以外）。当前为单租户生产站点（Supabase 项目 ref: `wbzdwwvtbaftjxecgdxk`，ap-northeast-1 区域，2026-06-02 从 Volcengine 迁移到官方 supabase.com）。

## 版本技术栈
- **Framework**: Next.js 16.1.1 (App Router) + Turbopack (SWC)
- **Core**: React 19.2.3
- **Language**: TypeScript 5.9 (target ES2022, strict)
- **UI 组件**: shadcn/ui (Radix UI) + lucide-react (图标，统一深蓝 `#1B2A4A`)
- **Styling**: Tailwind CSS 4 + tw-animate-css
- **包管理**: pnpm 9+ (严禁 npm/yarn，`preinstall` 钩子强制)
- **后端**: Supabase (Postgres 17, RLS) + Drizzle ORM 在 `storage-server/`（已废弃，仅供历史参考）
- **AI**: 火山引擎豆包 Ark (`@supabase/supabase-js` 2.95+)
- **字体**: Inter (next/font/google 自托管，**已不再**用 fonts.googleapis.cn)
- **认证**: Supabase Auth + `src/lib/supabase-auth.ts` (Bearer token 模式，admin/partner/role helpers)
- **部署**: Railway (`pnpm start`)，Coze 平台开发 (Node 24)

## 目录结构
```
├── public/                      # 静态资源 (hero-bg.jpg, why-study-china.jpg)
├── src/
│   ├── app/                     # 页面路由 + API
│   │   ├── layout.tsx           # 根布局 (metadata + Inter font + ClientLayout)
│   │   ├── page.tsx             # 首页 (RSC)
│   │   ├── globals.css          # 全局样式 (SICA 品牌色 + Tailwind 4)
│   │   ├── fonts.ts             # next/font/google 配置 (Inter)
│   │   ├── sitemap.ts           # SEO sitemap
│   │   ├── robots.ts            # SEO robots
│   │   ├── about/               # 关于 (RSC)
│   │   ├── contact/             # 联系 (RSC + ContactForm client island)
│   │   ├── assessment/          # 学术评估 (RSC + AssessmentForm client island)
│   │   ├── universities/        # 大学模块
│   │   │   ├── page.tsx         # 大学目录 (client, 搜索/筛选/分页; data on-mount fetch)
│   │   │   └── [slug]/page.tsx  # 大学详情 (client, tabs/gallery; data on-mount fetch)
│   │   ├── programs/            # 项目 (client, filters; data on-mount fetch)
│   │   ├── scholarships/        # 奖学金 (client, filters; data on-mount fetch)
│   │   ├── admin/               # 🔒 Admin 门户 (auth-gated, RSC + client islands)
│   │   │   ├── login/, register/ (后者用 ADMIN_INVITE_TOKEN 门控)
│   │   │   ├── dashboard/, students/, applications/, programs/
│   │   │   ├── universities/, scholarships/, fees/, leads/, assessments/
│   │   ├── partner/             # 🔒 Partner 门户 (auth-gated, 真实 Supabase auth)
│   │   ├── student/             # 🔒 Student 门户 (auth-gated, RSC + client islands)
│   │   └── api/                 # 17 API route groups:
│   │       ├── universities/, programs/, scholarships/   (public-ish)
│   │       ├── student/         (Bearer auth + requireUser)
│   │       ├── partner/         (requirePartner, partnerId 从 session 派生)
│   │       ├── admin/           (requireAdmin)
│   │       ├── ai/, applications/, assessments/, leads/, seed/, stats/
│   ├── components/              # 组件
│   │   ├── ui/                  # shadcn/ui 组件库
│   │   ├── client-layout.tsx    # 客户端布局 (I18nProvider + Header + Footer)
│   │   ├── header.tsx           # 顶部导航
│   │   ├── footer.tsx           # 页脚
│   │   └── university-logo.tsx  # 大学 logo (next/image)
│   ├── hooks/                   # use-mobile, use-student-api
│   └── lib/                     # 工具库
│       ├── data.ts              # 静态 fallback 大学/项目/奖学金 (9+17+10) + filter dropdowns
│       ├── i18n.tsx             # 客户端 i18n Context (cookie sync)
│       ├── i18n-translations.ts # 翻译表 (server-safe, 无 'use client')
│       ├── server-t.ts          # getServerT() - RSC 翻译 helper
│       ├── supabase-browser.ts  # 浏览器端 Supabase client (anon key)
│       ├── supabase-server.ts   # 服务端 Supabase client (service role)
│       ├── supabase-auth.ts     # getRequestAuth, requireAdmin, requirePartner
│       ├── api-client.ts        # apiFetch (浏览器) - 自动加 Bearer token
│       ├── auth-context.tsx     # AuthProvider + useAuth (admin/student/partner)
│       ├── student-data.ts      # 旧 mock 数据 (注意: 含 password 字段, S5 待清理)
│       └── utils.ts             # 通用工具
├── database/
│   ├── student-tables.sql      # 学生门户表 + RLS policies
│   ├── migration-supabase-cloud.sql  # 完整 schema 迁移 (94KB)
│   └── fix-rls-recursion.sql   # SECURITY DEFINER is_admin() 修复
├── scripts/
│   ├── build.sh, dev.sh, start.sh, prepare.sh, validate.sh
│   └── bootstrap-auth.sh       # 迁移后用 admin API 引导 auth.users
├── .env / .env.volcengine      # Volcengine 配置 (rollback 用，保留 7 天)
├── .env.example                # 环境变量模板
├── next.config.ts              # Turbopack + 远程图片 hostname: '*' + AVIF/WebP
├── tailwind.config / postcss.config / eslint.config / tsconfig
└── components.json             # shadcn 配置
```

## 构建和测试命令
- **开发**: `pnpm dev` (脚本启动 `pnpm tsx watch src/server.ts`，端口 5000，**Mac 上会被 ControlCe 占用**，改用 `pnpm next dev -p 5050`)
- **构建**: `pnpm build`
- **类型检查**: `pnpm ts-check` (0 errors 是硬性要求)
- **Lint**: `pnpm lint` / `pnpm lint:build`
- **生产启动**: `pnpm start` (走 `src/server.ts` 自定义服务器)
- **数据库迁移**: 在新 Supabase 项目 SQL 编辑器跑 `database/migration-supabase-cloud.sql`
- **引导 auth.users**: 迁移后跑 `bash scripts/bootstrap-auth.sh`

## Production / observability (Phase 34)

- **Health check**: `GET /api/health` — DB ping + env presence (Supabase / Resend / Sentry / cron secrets) + version (git SHA on Railway) + uptime. Returns 200 ok / 503 degraded. No auth — uptime monitor friendly. Wire Better Uptime / cron-job.org to it.
- **Error monitoring**: Sentry (`@sentry/nextjs@10.x`). Env-gated on `SENTRY_DSN` — unset = no SDK init, no build wrapping, no overhead. To go live: create a Sentry project, paste the DSN into Railway + `.env`, redeploy. The build-time `withSentryConfig` wrapper is also env-gated — empty `SENTRY_DSN` means plain Next build with no Sentry plugins. See `src/instrumentation.ts` + `next.config.ts`.
- **Cron schedule** (the two endpoints the runtime owns):
  - `POST /api/cron/generate-news` — daily news drafts (5 topics, ~3 min). Schedule once per day via Railway Cron / Vercel Cron / `cron-job.org` POST. Auth via `x-cron-secret` header matched against `NEWS_CRON_SECRET`.
  - `GET /api/email/drip-cron` — bounce-aware once an hour (the in-process `setInterval` from `src/server.ts` already runs every 5 min, so an external cron is a redundancy / SLA backstop, not required). Auth via `x-cron-secret` matched against `DRIP_CRON_SECRET`.
- **Custom server error capture**: `src/server.ts` has a single try/catch around Next's `handle(req, res)` and a `server.once('error', ...)`. Both lazily `import('@sentry/nextjs')` and `captureException` only when `SENTRY_DSN` is set. No new error falls through.
- **Email deliverability**: `ADMIN_EMAIL=admin@sica.cn`, `noreply@sica.com.cn` is the `From`. Verify SPF (TXT `sica.com.cn`), DKIM (Resend auto-publishes via their onboarding), DMARC (`_dmarc.sica.com.cn` TXT) before launching to real recipients. Resend activity log is the bounce/complaint source-of-truth.
- **Backups**: Supabase project → Database → Backups: Daily PITR is on by default for paid plans (free tier = daily logical backup, 7-day retention). Confirm under Settings → Database → "Point in time recovery" is enabled. To restore: Supabase dashboard → Backups → pick a snapshot.
- **GA4** (Phase 29): `NEXT_PUBLIC_GA_MEASUREMENT_ID` env-gated. Currently empty — set when the production domain is ready.

## 设计规范
- **品牌色**: Deep Crimson `#9B1B30`, Deep Dark Blue `#1B2A4A`, Gold `#D4A853` (仅排名徽章)
- **图标色**: 统一深蓝 `#1B2A4A`，**禁止多色图标**
- **背景色**: Warm White `#FAFAF8`
- **字体**: Inter, 自托管 via `next/font/google` (src/app/fonts.ts)
- **圆角**: 0px (直角)，按钮和卡片**禁止**使用圆角 (`rounded-none`)
- **详见**: DESIGN.md

## 国际化 (i18n)
- 支持 `en` (English) 和 `zh` (中文)
- Server: `await getServerT()` (从 `sica-locale` cookie 读 locale) - 用在 RSC
- Client: `useI18n()` Hook 提供 `t()`, `locale`, `setLocale()` - 用在 `'use client'` 组件
- 翻译表定义在 `src/lib/i18n-translations.ts` (server-safe)
- Client provider 在 `src/lib/i18n.tsx` 写 cookie (`sica-locale=...; max-age=31536000; SameSite=Lax`)
- 大学/项目/奖学金数据中英文字段分别存储 (如 `name`/`nameCn`)

## 数据层
- 静态 fallback 在 `src/lib/data.ts` (9 universidades, 17 programs, 10 scholarships, filter dropdowns)
- **运行时数据** 来自 Supabase (Postgres 17 + RLS)
- 11 业务表 + 1 sequence + 33 RLS policies + 11 updated_at 触发器 + 1 学生注册触发器
- `src/lib/supabase-server.ts` 返回 service-role client (用于 admin/seed 场景)
- `src/lib/supabase-auth.ts` 提供 per-request session-bound client (用 anon key + Bearer JWT)

## API 认证模式
**关键**: 任何 `/api/student/*`, `/api/partner*`, `/api/admin/*` 都通过 `src/lib/supabase-auth.ts` 校验

```ts
import { getRequestAuth, requireAdmin, requirePartner } from '@/lib/supabase-auth';

// 任何登录用户都能调用
const auth = await getRequestAuth(request);
if (!auth.ok) return NextResponse.json({ error: auth.error }, { status: auth.status });
const { supabase, user } = auth;  // session-bound client + verified user

// 仅 admin/super_admin
const auth = await requireAdmin(request);

// 仅 partner
const auth = await requirePartner(request);
// auth.partnerId 从 auth.users.id → partners.user_id 派生, 不可伪造
```

`/api/admin/create-user` 支持创建 admin/partner 角色; partnerId 永远从 session 派生, 不可从 query 传入

## 编码规范
- TypeScript strict 模式 (`pnpm ts-check` 必须 0 errors)
- **禁止** 隐式 `any` / `as any` (admin 表单里有 17 处遗留, S5 计划清理)
- 动态内容 (`Date`, `Math.random`) 必须 `useState + useEffect` 避免 hydration mismatch
- 页面组件如使用 hooks 需标记 `'use client'`
- 公共静态页面优先用 RSC, 客户端 island 只放交互部分
- 所有函数参数和返回值需明确类型
- 用 `next/image` + `sizes` 不用 raw `<img>` (老代码还有遗留, 已大部分修复)
- 列表/详情页 **不要** import `src/lib/data.ts` 里的完整数组 (会全部 bundle 到 client); 用 API fetch on-mount

## Hydration 注意事项
- Footer 中的年份使用 `useState + useEffect` 而非直接 `new Date().getFullYear()`
- 禁止 JSX 中直接使用 `typeof window`、`Date.now()`、`Math.random()`
- Cookie-based locale: server 和 client 必须用同一个 `sica-locale` cookie, 避免 hydration 警告
- `next/font` 自动加 `font-display: swap`, 不需要手动处理
- `suppressHydrationWarning` 在 `<body>` 上 (因为 cookie-based locale 切换时的微小差异)

## 已知遗留 (S5+ 计划)
- **M11**: 17 处 `as any` 在 admin 表单 (用 drizzle-zod 重构)
- **M3**: `src/app/student/page.tsx` 仍用 mock 数据 (`mockStudentStats`)
- **M2**: partner 页面 mock 数据 (布局已修, 页面数据未接 API)
- **L1**: `src/lib/student-data.ts` 仍含 `password: string` 字段
- **L8**: 0 测试 (需要 bootstrap vitest)
- **L10**: `application_number` 用 `Date.now()` 而非 `generate_application_number()` SQL 函数
- **L11**: partner/admin 门户 UI 字符串硬编码英文 (useI18n 覆盖率不完整)
- **L15**: `/api/proxy-image` 仍存在 (H6 未完全清理, 目前用 `unoptimized` next/image 替代)
- **S16** (2026-06-05): Student Portal Phase 1 — Save-as-Draft (POST accepts status='Draft' with looser validation; wizard has button on every step; resumes via `/student/applications/new?resume=<id>` using PUT), Withdraw (PUT allows `Draft/Submitted → Withdrawn` + confirmation dialog), Resubmit (PUT allows `Documents Requested → Under Review` and `Rejected → Submitted` + auto-stamps `submitted_at` + timeline note), adminNotes surfaced in detail-page banner, `StudentApplication.adminNotes` field added. PUT allows editing university/program/degree/intake while row is in `Draft` state.
- **S17** (2026-06-05): Student Portal Phase 2 — sidebar notification badge on Applications item (red for `Documents Requested`, gold for `Draft`, hidden if zero), refetches on route change and window focus. In-page action banner on dashboard + applications list, with per-filter counts on the Docs Needed / Draft filter chips.
- **S18** (2026-06-05): Partner Applications enhancement — DB migration adds 7 columns (`student_email`, `student_phone`, `intake`, `degree`, `nationality`, `priority`, `application_number`) + per-partner counter table + `next_partner_app_number()` RPC that mints `PA-YYYY-NNNN` IDs. New/edit forms capture the new fields and auto-fill contact info when picking from `partner_students`. Detail page shows student contact card (email/phone/nationality) and a Quick Status Update panel with the partner-allowed transitions (Draft↔Submitted, →In Review, →Accepted/Rejected, Withdrawn, Reopen). Each transition opens an `AlertDialog` confirmation with per-(from,to) copy: destructive ones (Rejected/Withdrawn) get a red confirm button + explicit "this signals X" wording, routine ones stay calm. List page adds a Priority column + Urgent/High stat card + priority filter + new columns surfaced under Student (email, intake·degree, application #). Export button wired up: new `/api/partner/applications/export` returns RFC 4180 CSV with UTF-8 BOM, 17 columns, scoped to partner's team. **Security hardening (S18b)**: PATCH route now enforces `PARTNER_STATUS_TRANSITIONS` server-side (the allow-list lives in `partner-application-mapper.ts`, imported by both the UI and the API so there's a single source of truth). Disallowed transitions return HTTP 400 with the list of valid next states. Same→same is a no-op short-circuit. Auto-stamps `submitted_at` on the first move to Submitted / In Review.
- **S19** (2026-06-05): Public filter system overhaul — `/universities` and `/programs` get URL-synced filter state via new `useUrlState` hook (`src/hooks/use-url-state.ts`). Refresh-survives filters, shareable `?city=Beijing&tag=985&rating=4.8` links, browser back/forward. Both pages render active filter pills with X-to-remove + a "Clear all" link. New filters: `tag` (985/211/DFC) + `type` (Public/Private) + `minRating` (4.5★+ / 4.7★+ / 4.8★+ / 4.9★) on universities; `university` (specific school) + `scholarship` boolean toggle on programs (14 of 32 programs are scholarshipAvailable — this is the #1 student question). New "Rating" sort on universities, "Name" sort on programs. Fixed the "popularity" no-op sort on programs — renamed to "Default" (preserves API insertion order). Removed the dead "Search" button on universities (filters already fire on every keystroke). Empty state rewritten with reset CTA + suggestion copy. New i18n keys in `filter.*` namespace.
- **S20** (2026-06-05): Student portal + partner new-app data quality — (1) student profile was hardcoded "John Smith"; rewrote to fetch + PUT `/api/student/profile` with the real DB columns (passport, GPA, target intake, target field, etc.). (2) New `SearchableSelect` component (Popover + Command) used in the application wizard + partner new-app for type-to-search university/program pickers. (3) The student application wizard used to import `universities` + `programs` from the static 2,300-line `data.ts` fallback — admin-added universities and programs were invisible. Replaced with live `/api/universities` + `/api/programs` fetches so admin changes appear in the wizard immediately. (4) Fixed a doc-shape bug where the initial mount of the wizard read `d.documents` from `/api/student/documents` while the API returns `{ data: [...] }` — the wizard started with zero docs even when the student had uploaded some. (5) Documents uploaded in the wizard step 2 are now linked to the new application via a follow-up PATCH to each doc's `application_id` (previously they were orphans). (6) `/api/student/documents/[id]` PATCH now maps `applicationId` (camelCase) → `application_id` (snake_case); explicit null unlinks. (7) Student `/student/documents` page now shows a "Linked to" badge per doc + a "Link to" dropdown for retroactively attaching orphan docs to an application. (8) Partner new application replaces plain text university + program inputs with the same SearchableSelect pickers (so partners can't mistype "Tsingha University" and lose the row).
- **S21** (2026-06-05): Student profile → wizard prefill — student applications are no longer a blank-form re-typing exercise. On wizard mount, fetch `/api/student/profile` in parallel with the university + program lists. If the profile has a target_degree / target_intake / gpa / english_score / highest_education, auto-fill the matching wizard fields (degree level, intake, personal statement). Render a small "from your profile" badge next to each prefilled field + a step-1 banner that counts how many fields were pre-filled + an "Edit your profile →" link. The personal statement is seeded as a 2–3 sentence draft from the profile (GPA + education + English score + goal) with an explicit "[Edit this draft — ...]" footer, so the student sees it's a draft not a finished statement. The badge disappears the moment the student types in the field (we track prefilledFields in a Set).
- **S22** (2026-06-05): Wizard step 1 collapsed to a single program picker. The old 2-picker layout (university + program) was two clicks and two chances to mismatch. The new layout is a single `SearchableSelect` over programs, where each row shows the program name + university name + degree + language, with a small circular university logo on the left. Typing either a program name OR a university name filters the dropdown (the cmdk `value` is the concat of label + sublabel). Picking a program auto-fills `targetUniversity` from `program.universitySlug`. The standalone University `SearchableSelect` is gone. The step-1 grid is now a single column (Degree Level → Program → Intended Intake). The `SearchableSelect` component itself learned to render an optional `logo` thumbnail in both the trigger and each dropdown row, with an `onError` handler that hides the image gracefully (for admin-added universidades that don't have a logo uploaded yet).
- **S23** (2026-06-05): Wizard shows ALL programs (no auto degree-level filter). The student wanted to see the full catalog in the program picker, not a pre-filtered subset based on the profile's `target_degree`. Removed the degree-level filter from the `useMemo` that built the option list; renamed `filteredPrograms` → `availablePrograms` to make the intent clear. The dropdown now renders all 32+ programs from the live API regardless of degree. Typing in the search box still filters to the right one — the cmdk filter is typeahead-fast on 35 rows.
- **S24** (2026-06-05): Bulk program import hardened. Two failure modes the user hit: (1) pasting a CSV with two rows for "BSc in Computer Science" at the same school would emit duplicate slugs and the INSERT would fail the whole batch — now we dedupe within the batch by appending `-2`, `-3`, etc. (2) re-importing the same list a week later would hit `unique constraint program_slug_key` and roll back everything — now we use Supabase `upsert()` with `onConflict: 'slug'` + `ignoreDuplicates: true` so existing rows are silently skipped. Response shape went from `{ imported, errors: [] }` to `{ imported, skipped, submitted, errors: [] }` and the UI shows the breakdown: green "X imported", yellow "Y already existed (skipped)", plus the validation errors as before.
- **S25** (2026-06-06): Dynamic intakes. Admin can now manage intake periods (e.g. "2026 Fall", "2027 Spring") under `/admin/intakes` — add/edit/delete, with a "Is active" toggle that controls which intake shows in the student wizard's Intended Intake dropdown. Migration `database/2026-06-06_intakes.sql` adds `intake_periods` table with RLS, then we refactored the student wizard to call `/api/intakes?active=true` instead of reading from a hardcoded array. Also fixed a 1-character typo: API returns `universities` (English) but the partner form was reading `universidades` (Spanish), so university names rendered as `undefined` for every row picked from the live API. Renamed `linkPendingDocsToApplication` → `linkOrphanDocsToApplication` to match the docs-page vocabulary.
- **S26** (2026-06-06): Partner new-app form overhaul. 9 sections (Student Profile / Contact / Academic / Program / Financial / Documents / Workflow / Internal / Review) with sticky bottom submit bar; 26 new DB columns (contact: `student_email`, `student_phone`, `student_dob`; academic: `gpa`, `english_score`, `english_test_type`, `highest_degree`, `target_degree`, `target_intake`, `university_slug`, `program_slug`; financial: `application_fee`, `paid_amount`, `currency`, `commission_eligible`, `expected_commission`; documents: `documents_complete`; workflow: `priority` enum, `tags` text[]; internal: `commission_notes`, `student_source`, `assigned_to`; metadata: `notes`); 7 closed-set enums (`G7_PRIORITY`/`G7_DOC_COMPLETE`/etc.) constrained via Zod on the API and Select dropdowns on the UI. Both create + edit routes now share `src/components/partner/PartnerApplicationForm.tsx` (1115 lines) — same 9 sections, same validation, single source of truth. CSV export at `/api/partner/applications/export` now returns 17 columns (was 13) and is scoped to the partner's team only. Migration `database/2026-06-06_partner_applications_enrich_v2.sql` applied via Supabase Management API.
- **S27** (2026-06-06): Partner status is admin-only. Removed Status/Decision Selects from `PartnerApplicationForm` Workflow section. Partner PATCH/POST reject `status`/`decision`/`submittedAt` with 403; mapper strips them as safety net. Removed Quick Status Update panel + `StatusChangeDialog` from partner detail. Built admin side: `src/app/api/admin/partner-applications/route.ts` (GET list) + `[id]/route.ts` (GET + PATCH) + admin list + detail pages; new "Partner Pipeline" nav item between Partners and Fees in the admin sidebar.
- **S28** (2026-06-06): Unified student + partner applications in admin `/applications`. `/api/admin/applications` GET now merges `student_applications` + `partner_applications` into one list with `surface: 'student' | 'partner'` field. New `surface` + `'Partner CRM'` source value on `AdminApplication`. Counts endpoint adds `partnerCrm` count; total = 15 (3 student + 12 partner CRM). Page: orange "Partner CRM" badge, Partner tab tooltip with breakdown, checkbox column, View Details routes by surface, Delete routes by surface. Added DELETE handler to `/api/admin/partner-applications/[id]`.
- **S29** (2026-06-06): Notify student + partner on partner-app status change (fixes S27's silent path). Migration `database/2026-06-06_partner_app_timeline_and_notifications.sql`: nullable `partner_application_id` FK on `application_timeline` + CHECK constraint (exactly one of `application_id`/`partner_application_id` is set), RLS for partners, new `partner_notifications` table, 8 new `email_templates` rows (`status.withdrawn` + 6 `status.*.partner` slugs). `insertTimelineEvent` accepts either `application_id` or `partner_application_id` (validates XOR). Refactored `src/lib/email.ts` to share `sendStatusEmail()` between applicant + partner paths; new `notifyPartnerOnStatusChange()` with `PARTNER_STATUS_TO_TEMPLATE_SLUG` (maps partner's `'In Review'` → student's `'Under Review'` template); new `wrapForPartner()` HTML wrapper with "as a SICA partner agency" footer. Partner admin PATCH on status change: timeline event + email student + email partner + `partner_notifications` row (with `link_url=/partner/applications/[id]`). Student admin PATCH now also inserts `student_notifications` row (table was empty since v1). **Bug fixed**: `service.auth.admin.getUserById` returns `{data:{user}}` not `{data:user}` — was silently null on both GET (createdByEmail) and PATCH (partner email/notification) paths. **Bug fixed**: `partners.name` column doesn't exist (it's `company_name`). **Bug fixed**: `text[]` vs `jsonb` in `email_templates.variables` by using `jsonb_build_array()`.
- **S30** (2026-06-06): Partner notifications inbox + sidebar bell badge. API: `/api/partner/notifications` (GET list with unreadCount), `/[id]` (PATCH mark-read), `/read-all` (POST), `/unread-count` (GET). Page `/partner/notifications` with All/Unread filter, mark-all, optimistic updates, 30s polling + focus/filter refetch. Partner sidebar: new "Notifications" nav item between Dashboard and Students with red unread badge, polled every 30s.
- **S31** (2026-06-06): Admin bulk operations on Applications. API `/api/admin/applications/bulk` POST: actions `status`/`priority`/`note`/`delete`, caps 200 rows, surface-aware routing, fires S29 side-effect chain per row, returns `{updated, failed:[{id,error}]}`. Status name union shown in picker; cross-taxonomy writes rejected per row. Page: checkbox column (first), select-all header (indeterminate), per-row checkbox, selected rows get crimson tint, sticky bottom action bar with 4 actions (Change status / Set priority / Add note / Delete), 3 confirm dialogs, result dialog with failure list. Selection auto-clears on tab/filter change.
- **S32** (2026-06-06): Student notifications inbox + sidebar bell badge (mirror of S30). API: `/api/student/notifications` (GET), `/[id]` (PATCH), `/read-all` (POST), `/unread-count` (GET); all scoped to `student_id = user.id` from JWT. Page `/student/notifications` with All/Unread filter, mark-all, optimistic updates, 30s polling. Student sidebar: new "Notifications" nav item between Dashboard and My Profile with red badge; two badge systems coexist (existing Applications attention badge + new unread badge).
- **S33** (2026-06-06): Admin bulk CSV export. API `/api/admin/applications/export` GET: unified student + partner CSV, 20 columns (Application #, Surface, Student Name, Email, Phone, Nationality, DOB, University, Program, Degree, Intake, Status, Decision, Priority, Source, Submitted At, Created/Updated At, Notes), accepts `?ids=` for selected-only + same `?source/?status/?search/` filters as list endpoint, capped at 1000 rows with `X-Row-Count`/`X-Truncated` headers, RFC 4180 escaping, UTF-8 BOM for Excel, filename `sica-applications-YYYY-MM-DD.csv`. Page header: "Export CSV" button (outlined, opens with current filter set in new tab). Bulk action bar: "Export selected" button (uses `?ids=` with current selection, clears selection after). **Bug fixed mid-build**: `student_profiles.gender` column doesn't exist — removed from SELECT. Verified live: 15 rows on default export (matches S28 unified count), 3 rows on `?ids=...` export, proper CSV with header + UTF-8 BOM.
- **S41** (2026-06-06): News automation — 5 AI drafts per day from a curated topic queue. Migration `database/2026-06-06_news_automation.sql` adds `news_automation_topics` (queue: status, priority, post_id, etc.) + `news_automation_runs` (audit log per cron tick) + 14 evergreen seed topics. `src/lib/ai/news-automation-runner.ts` is the shared run logic (claim → generate → insert → audit) used by both the cron endpoint and the admin "Run now" button. Stale-claim recovery (1h+), 3× retry with 1/2/4s backoff, slug-collision guard, email admin on full failure. Refactored `src/lib/ai/blog-prompts.ts` + `src/lib/ai/blog-sanitize.ts` out of `/api/ai/generate-blog` so the cron and the interactive admin streaming endpoint share the same S36 SEO+AEO+GEO prompt + S35 third-party agency scrub. `src/app/api/cron/generate-news/route.ts` uses `x-cron-secret` auth (NEWS_CRON_SECRET env, dev-friendly unauthenticated when unset). Admin API: `GET /api/admin/news/automation` (dashboard), `POST /api/admin/news/automation/topics` (add), `DELETE /api/admin/news/automation/topics/[id]` (remove; 409 if in-flight), `POST /api/admin/news/automation/run` (manual trigger). `src/app/admin/news/automation/page.tsx` is the admin UI: summary cards, pending queue (add/remove), recent activity, run history with expand-to-see-errors. Sub-nav so Posts and Automation share navigation. `docs/news-automation.md` covers scheduler setup (Railway Cron / Vercel Cron / GitHub Actions / cron-job.org) + tuning + troubleshooting. **Bug fixed mid-build**: PostgREST schema-cache lag made `UPDATE…ORDER BY…LIMIT` claim fail with "column priority does not exist" — replaced with two-step `SELECT` then `UPDATE-WHERE-IN`, bulletproof against cold schema caches. Live tested: count=1 run (28s, 1 draft), count=5 run (178s, 5 drafts, 3-4 min read time each, 4-5 FAQ entries, S36 structured fields populated, all `status='draft'` in news_posts, 3 run rows in audit log all `status='success'`).
- **S42** (2026-06-06): Fix broken partner dashboard. Was calling `/api/partner-students`, `/api/partner-applications`, `/api/partner-fees` (old hyphen convention) — every partner saw a permanent error banner on first load. Switched to `/api/partner/students` and `/api/partner/applications` (new slash convention used by every other partner page), updated response-shape unwrapping (`{students: [...], total, page, limit, totalPages}` wrappers + camelCase field names), and dropped the "Pending Fees" stat card + "View Fees" button (the `/partner/fees` page was removed in Phase 3; link was 404). Removed the unused `PartnerFee` interface, `formatCurrency` helper, `DollarSign` import, and `fees` state. Live: 3 stat cards now show real DB counts; Recent Students + Recent Applications render correctly.
- **S42b** (2026-06-06): Student-portal polish batch (4 fixes from the S42 portal audit). (C) Disabled "Upload Document" button on application detail was permanently grayed out with a "not yet wired" tooltip on the exact page where students most want to upload — wired to deep-link to `/student/documents?applicationId=<id>`. The documents page now reads the param, filters to that application, and shows an amber "Filtered to application: <university>" banner with an X to clear. (F) Documents page now renders `rejection_reason` in a red callout under each Rejected doc (was being silently dropped even though the API returned it). (E) Per-step validation in the student wizard: Step 1 (degree / program / intake) now disables the Next button until all three are picked, with an inline red hint above the button explaining which one is missing. (D) Student Settings page was a complete stub (fake email, "last changed 3 months ago", non-functional switches, dead language toggle) — replaced with a real version: Account shows real Supabase email + working "Send reset link" button (Supabase `resetPasswordForEmail`), Notifications replaces fake switches with a "status emails go to your inbox + in-app" message, **Language card is actually wired to `useI18n().setLocale()` (first time the picker has worked since shipping)**, Timezone derived from `Intl.DateTimeFormat()` instead of hardcoded "EST", Security explains Supabase manages 2FA/sessions rather than faking the controls, Delete Account directs to `support@sica.com.cn` (in-app GDPR flow needs an admin-confirm backend; deferred). All 4 touched pages return HTTP 200.
- **S43a** (2026-06-06): i18n sweep — student portal. 145 new translation keys (en + zh) across 6 namespaces (`studentAppDetail.*`, `studentDocs.*`, `studentNotif.*`, `studentWizard.*`, `studentProfile.*`, `studentNav.*`) + a few `common.*` additions. `useI18n().t()` signature now accepts a `params` arg for `{{name}}` placeholder substitution (mirrors the existing server-safe `t()` in `i18n-translations.ts`). 7 student files converted: layout (sidebar + nav), login, register, applications list, applications/new (wizard, 66 t() refs), applications/[id] (detail, 64 refs), documents (31 refs), notifications (18 refs), profile (32 refs). Wizard per-step validation error now localizes via `t('studentWizard.requiredError', { field: ... })`. DB enum values (status, document category) stay untranslated to keep the DB ↔ UI contract stable. `pnpm ts-check`: 0 errors. Live: toggling to 中文 now translates the entire student portal.
- **S43b** (2026-06-06): i18n sweep — partner portal. 617 new translation keys (en + zh) across 12 namespaces (`partnerDashboard.*`, `partnerNav.*`, `partnerLayout.*`, `partnerLogin.*`, `partnerRegister.*`, `partnerAcceptInvite.*`, `partnerPending.*`, `partnerRejected.*`, `partnerApps.*`, `partnerAppNew.*`, `partnerAppDetail.*`, `partnerAppEdit.*`, `partnerAppForm.*`, `partnerStudents.*`, `partnerStudentNew.*`, `partnerStudentDetail.*`, `partnerStudentEdit.*`, `partnerLeads.*`, `partnerLeadNew.*`, `partnerLeadDetail.*`, `partnerTeam.*`, `partnerSettings.*`, `partnerNotif.*`) + shared `partnerCommon.*`. 22 partner files + the 1075-line `PartnerApplicationForm` (133 t() refs across 9 sections). DB enum values (status, priority, decision, document category, degree, etc.) keep their enum spelling; only human-readable badge labels get translated. Time-ago strings split into 4 separate keys. Dynamic "added by {{email}} on {{date}}" uses two separate keys so natural-language ordering stays correct in Chinese. `pnpm ts-check`: 0 errors. All 13 partner routes return HTTP 200. Live: toggling to 中文 now translates the entire partner portal — sidebar, role badge, status banners, dashboard, login/register/accept-invite, pending + rejected landings, applications (list + new + detail + edit), students (list + new + detail + edit), lead sharing, team, settings, notifications.
- **Hide news thumbnails** (2026-06-06): Removed `cover_image` rendering from `/news` index, `/news/[slug]` detail, and the home page news widget. Cover images are still set in the DB (admin can upload), but no surface shows them — a deliberate signal that we don't want stock-photo-driven editorial. The detail page still uses `featured_image` for hero treatment (only set on evergreen pillar posts).
- **Phase 1 quick wins** (2026-06-06): Five low-effort fixes the portal audit surfaced. (1.5) Click-to-sort column headers on `/partner/applications` list (created_at, updated_at, student_name) — `<SortHeader>` sub-component renders a 3-state sort icon (asc / desc / unsorted), server-side via `?sort=&order=` query params so the URL stays bookmarkable. (1.7) Resend / cancel invite on `/partner/team` — `POST /api/partner/team/[id]/resend` regenerates the invite token and re-sends the accept-invite email via Resend (new users still see the setup-password flow; existing users land on login). (1.9) Working "Send reset link" in `/partner/settings` — `POST /api/partner/me/send-reset` calls `supabase.auth.resetPasswordForEmail` with `redirectTo: ${SITE_URL}/login?reset=1`. (1.13) Show assigned reviewer on `/partner/applications/[id]` — `GET /api/partner/applications/[id]/reviewer` pulls the latest admin-actor from `application_timeline`, hydrates the email via `auth.admin.listUsers`, fall-soft to "SICA Admissions Team" if no admin has touched the row.
- **Phase 1.10 + 1.11** (2026-06-06): (1.10) `/student/documents` got a status filter (All / Pending / Verified / Rejected) + sort dropdown (Date uploaded / Name / Status, asc/desc toggle). Client-side sort after category + status + URL `?applicationId=` filters. 7 new i18n keys. (1.11) Date-grouped notification inboxes for both student and partner. New shared `src/components/ui/date-grouped-list.tsx` (generic `<DateGroupedList<T>>`). Buckets Today / Yesterday / This Week / Earlier with a header per bucket showing the count. Used on both `/student/notifications` and `/partner/notifications`.
- **Phase 1.12** (2026-06-06): `student_id` FK on `partner_applications` replaces the soft `student_name` join. Migration `database/2026-06-07_partner_applications_student_id.sql` adds a nullable `student_id` column with FK to `partner_students.id` + partial index `WHERE student_id IS NOT NULL`. Backfill via case-insensitive name match (5/12 rows auto-linked, 7 intentionally left NULL for the partner to reconcile). Mapper gets `studentId` field. `GET /api/partner/applications` accepts `?studentId=`. Student detail page now uses `?studentId=<uuid>` instead of `?search=<name>` so the filter survives name changes.
- **Phase 1.4 + 1.1** (2026-06-06): Bulk operations + pagination on partner Applications. (1.4) Bulk priority (High/Normal) + bulk delete via checkbox selection + sticky bottom action bar mirroring the admin S31 pattern. New `POST /api/partner/applications/bulk` (200-row cap, member-role can only delete rows they created). (1.1) Pagination via "Load more" button (no page numbers — partner user base is small enough that "more" is clearer than a jump-to-page). New `GET /api/partner/applications` returns `{applications, total, page, limit, totalPages}` + accepts `?page&limit`. Page render wrapped in a fragment so the sticky bulk bar is a sibling, not a child of the card.
- **Phase 1.1 student + 1.2 + 1.3** (2026-06-06): (1.1) Student-side pagination. `GET /api/student/applications` now returns the same `{applications, total, page, limit, totalPages}` shape + `?page&limit` params; the student page renders a "Load more" button. Fixed a long-standing typo on `/student/applications` — the page was reading `data.applications` while the API returned `{ data: [...] }`, so the list silently rendered empty for every student. (1.2) `link_url` column on both `student_notifications` and `partner_notifications` (migration `database/2026-06-07_notification_link_url.sql`). Admin single PATCH + bulk route now write `link_url: /student/applications/<id>` on status-change. (1.3) Student dashboard shows the latest 3 notifications as a new card (links to `link_url` or `/student/notifications` as fallback).
- **Phase 1.6** (2026-06-06): Per-field validation in `PartnerApplicationForm` (the 1075-line shared form). The form had a single `error: string` state at the top — submit would say "Student name is required." at the top of a 9-section form, partner scrolled to the top to see what failed, fixed the name, re-submitted, then got "University required." This change adds a `fieldErrors: Record<string, string>` prop the parent populates on failed validation. The form renders each error inline under its field with a red border on the input. Currently validated: `studentName`, `university`, `program`, `passportExpiryDate < passportIssueDate` cross-check. New i18n key `partnerAppNew.errorPassportDateOrder`. Submit flow: validate → collect all errors → if any, scroll to first + focus it; otherwise proceed. **Bonus fix**: parents were passing `universities={universities}` (with 'e') but the prop type said `universidades` (with 'i') — the form was silently using `undefined` as the universities array, so the program SearchableSelect's option mapper was calling `undefined.find` and surviving only because the program list was empty. Fixed both parents + collapsed a duplicate `className` attribute on the passport-expiry input. `pnpm ts-check`: 0 errors.
- **Phase 1.8** (2026-06-06): "Convert Lead to Student" CTA on `/partner/lead-sharing/[id]`. The lead-sharing page tracks incoming inquiries before they become students or applications, but the `Converted` status had no action behind it — partners re-typed the lead's name + email into Students, then again into a new application. New `POST /api/partner/leads/[id]/convert` endpoint atomically creates a `partner_students` row from the lead's name/email/phone/notes + flips the lead to `Converted` + returns the new student id. The form then routes to `/partner/students/<new-id>` so the partner can immediately start an application. New `UserPlus` icon button in the lead-detail action bar (gated on `lead.status !== 'Converted'`). Confirmation dialog explains the side-effect ("lead will be marked as Converted") before clicking. Emerald success banner before navigation. `target_university` / `target_program` left NULL — they're slugs in `partner_students` but freeform text on the lead, so we don't guess. 5 new i18n keys (en + zh) under `partnerLeadDetail.*` (`convert` / `converting` / `convertTitle` / `convertBodyFor` / `convertSuccess` / `convertError`). `pnpm ts-check`: 0 errors.
- **Phase 2** (2026-06-06): Admin document review queue. `student_documents` had `status` / `verified_at` / `verified_by` / `rejection_reason` columns but **no admin endpoint to flip the status** — every uploaded doc stayed `Pending` forever. This is a real workflow gap: students wait, admins have no UI to clear the queue. New API: `GET /api/admin/documents` (filter status / search / studentId / applicationId, sort by uploaded_at / name / status / category, paginated); `GET /api/admin/documents/[id]` (single + student join); `PATCH /api/admin/documents/[id]` (set status + rejection_reason, stamps verified_at/verified_by, clears reason on approve/move-back); `POST /api/admin/documents/bulk` (200-row cap, single status payload, per-row notification, returns `{updated, failed[]}`). New UI: `/admin/documents` (filter chips, debounced search, table with checkbox + sticky bulk bar mirroring S31 + Phase 1.4 patterns, pagination); `/admin/documents/[id]` (student card, file preview with image / PDF iframe / external link, application linkage, current status, rejection reason read-only, action buttons, audit card with uploaded_at / verified_at / verified_by, reject dialog with required reason). Side effects on every status change: insert `student_notifications` row (type='document_review') with `link_url=/student/documents?applicationId=<id>` so the student gets a bell-badge + lands on the filtered doc list. Notifications are best-effort — logged but don't fail the PATCH. Admin sidebar: new `Documents` entry (FileCheck icon, bilingual label). Admin layout now wraps in I18nProvider so admin pages can `useI18n()` going forward (none did before — they all hardcoded English). 33 new i18n keys (en + zh) under `adminDocs.*`. Live tested end-to-end: approve / reject (with and without reason — 400 if missing) / move back to pending / bulk approve (5 docs updated, 5 notifications inserted) / no-auth (401). `pnpm ts-check`: 0 errors. Original 5 Pending test docs restored to Pending state after smoke test.
- **Phase 3** (2026-06-07): AI chatbot now reads the live Supabase catalog. The bot had three hardcoded lists of universities (in `src/lib/ai/prompts.ts` system + university context prompts, and in `src/lib/ai/knowledge.ts` `searchUniversities`) that mirrored the static fallback in `src/lib/data.ts` — 9 universities. The live DB has **27 universities + 149 programs + 10 scholarships**, so anything the admin added through `/admin/universities` (Tianjin, Yangzhou, Xuzhou Medical, etc.) was invisible to the bot. New module `src/lib/ai/live-data-context.ts` fetches universities (top 30 by ranking), programs (grouped by discipline, 3 per discipline, capped at 24), and scholarships (top 20) from Supabase via `getSupabaseServer()` with a 5-min in-memory cache. Renders a compact RAG block (~12.6KB for current 27 unis + 24 programs + 10 scholarships) injected as a new `## SICA Live Catalog Context` section in the system prompt. Graceful fallback to the static data.ts arrays if Supabase is unreachable, so the bot never goes dark. Freshness footer so the LLM knows the data isn't real-time. `buildRAGContext()` and the rule-based fallback path both use the live data — the per-uni name match in `generateIntelligentResponse` can now recognize any school in the catalog. Removed the hardcoded 9-university list from `SICA_CHATBOT_SYSTEM_PROMPT` and emptied `SICA_UNIVERSITY_CONTEXT_PROMPT` (was dead weight, ~3k tokens). Live tested: "Tell me about Tianjin University" → LLM cites founding year (1895), location (Tianjin, near Beijing), ranking — none of this is in the static 9, all from the live catalog. `pnpm ts-check`: 0 errors.
- **Phase 4** (2026-06-07): On-demand detail injection + working cards for every school. Phase 3's one-line catalog summary left two gaps: (1) the catalog block didn't include slugs, so the LLM could only emit `[[CARD:university:<slug>]]` for the original 9 hardcoded slugs, not the 18 admin-added schools; (2) the one-liner was enough to confirm a school exists, but couldn't answer rich questions like "What programs does Tianjin University offer?" or "How much is tuition and when's the application deadline?". Fix: `formatUniversityLine` / `formatProgramLine` now append `(slug: \`<slug>\`)` so the LLM can render a card for any school/program in the catalog. New `getDetailContext(userMessage)` in `live-data-context.ts` matches the user's message against university/program/scholarship names (longest-match-first so "Tianjin University" doesn't get swallowed by a partial match) and fetches full records from Supabase for the matches — capped at 2 unis, 2 programs, 2 scholarships. University detail block includes ranking, QS world, type, established, students, disciplines, popular programs, tuition (undergrad + grad), intake, **application deadline**, accommodation, scholarship info, description (capped 600 chars), tags, and up to 12 of the university's programs. Program detail includes parent university, degree, language, duration, tuition, discipline, description. Scholarship detail includes type, coverage, eligible degree levels, regions, duration, deadline, requirements. Empty when nothing matched, so generic questions don't pay the per-message cost. Live tested: "Tell me about Tianjin University" → 1 tianjin-university card + full detail (founded 1895, ranked #13, tuition ¥25-40k, deadline, accommodation). "What programs does Tianjin University offer?" → 3 program cards (chemical-engineering, environmental-engineering, pharmaceutical-science). "Compare Tianjin University and Yangzhou University" → both uni cards, no detail bloat. "Tell me about Tsinghua University" → still works (regression check). "How much does it cost to live in China?" → no cards, no detail (just the live catalog + FAQ). `pnpm ts-check`: 0 errors.
- **Phase 5** (2026-06-08): Create partner team member with email + password. The team invite flow had only one path: send a Resend email with an accept-invite link, the recipient clicks it, sets a password, and is active. Two real-world gaps: (1) the Resend free tier caps out and the email never arrives; (2) small internal team where the owner already knows the member's email + a starting password and the invite round-trip is pure ceremony. New `POST /api/partner/team/create-with-password` accepts `{email, password, fullName?, role='member'}`, validates (email + password ≥8 chars + role=member), refuses if email already exists in auth.users (409 with a hint to use the email-invite flow instead), creates the auth.users row with `email_confirm: true`, inserts a `partner_team_members` row with `status='active'` + `joined_at=now` immediately. No email is sent. The new member can sign in at `/partner/login` right away. The password is returned in the response so the UI can display it once for the owner to copy. UI: tabbed modal on `/partner/team` with "Email invite" (default, unchanged) and "Create with password" (new) tabs. Password tab: email + password + optional full name inputs + hint text. After successful create, the modal shows the email + password in read-only fields with per-field Copy buttons (uses `navigator.clipboard` with textarea fallback) and an emerald success banner explaining the password won't be shown again. Unified `closeInviteModal` helper resets the modal state on close. 14 new i18n keys (en + zh) under `partnerTeam.*`. Live tested: happy path (new user can immediately sign in with owner-set password), existing email → 409, password <8 → 400, invalid email → 400, no auth → 401, `/partner/team` page → HTTP 200, test member created + removed cleanly via DELETE. `pnpm ts-check`: 0 errors.
- **Phase 5b (bugfix)** (2026-06-08): Team members got 403 "No partner account bound to your user" on login. Phase 5 created a `partner_team_members` row with `status='active'` for new team members, but no row in `partners` (the org table) — which is the correct design: partners = org, partner_team_members = membership. The login flow + most partner APIs were hard-coded to look up via `partners.user_id` first, so every team member (whether created via the email-invite or the new direct-create flow) hit 403 on every API call. The partner login page signed them out, leaving them unable to enter the portal at all. Pre-existing bug masked by the invite flow (probably no one completed an invite + login in dev). Fix: `requireTeamMember` + `requirePartner` in `src/lib/supabase-auth.ts` now try both paths — (a) `partners.user_id = auth.uid()` (org owner, legacy direct-link) checked first for back-compat; (b) `partner_team_members.user_id = auth.uid()` joined to `partners` via `partner_id` (team member). `GET /api/partner/me/route.ts` got the same two-path lookup so the partner login page stops 403-ing. Live tested: team member created via direct-create can sign in, `/api/partner/me` returns 200 with the joined partner org, `/api/partner/students` returns 200, owner path still works (regression), admin gate still 403s partners. `pnpm ts-check`: 0 errors.
- **Phase 6** (2026-06-08): Partner signup recovery flow + ziovomart 403 fix. The partner signup flow at `/partner/register` does the work in two steps: (1) Supabase `signUp()` creates the auth.users row, (2) `/api/partner/signup` creates the partners + team_members rows. If step 1 succeeds but step 2 fails (network error, user closes the tab mid-form, validation error, etc.), the user ends up with an auth account that has `role='partner'` in metadata but no partner/team rows. They can sign in, but every partner API 403s them with "No partner account bound to your user" and the login page signs them out. They're stuck — the register form won't accept the same email again (Supabase signUp refuses duplicates) and there's no recovery path. This is what happened to ziovomart@gmail.com: auth row was created at 2026-06-08 08:39 with role='partner', but no partners or team_members row. New `POST /api/partner/complete-setup` (auth: any logged-in user; validates role='partner' + no existing partner/team row; body: company_name, contact_person, phone, country, notes?; creates partners row with status='Active' + team_members row with role='owner'/status='active'; rolls back the partner insert if the team insert fails; returns `{partner, teamMember}`). `GET /api/partner/me` now detects the half-signup state and returns 409 with `code='PARTNER_SETUP_INCOMPLETE'` instead of the generic 403 — the non-half-signup 403 path is unchanged. UI on `/partner/login`: when `/api/partner/me` returns the new 409, the page swaps the login form for a "Complete your partner registration" form (5 fields: company, contact, phone, country, notes). On submit success, routes to `/partner`. "Use a different account" link signs out + resets to the login form. 13 new i18n keys (en + zh) under `partnerLogin.*`. Also fixed ziovomart directly: created a partners row (company='Ziovomart Partner Org', contact='Ziovomart Admin', status='Active') and a team_members row (role='owner', status='active') via direct DB insert — they can log in now and rename via `/partner/settings`. Live tested: fresh half-signup user → 409 PARTNER_SETUP_INCOMPLETE → complete-setup → 200 + partner loaded; re-running complete-setup → 409; student user trying complete-setup → 403. `pnpm ts-check`: 0 errors.
- **Phase 7** (2026-06-08): Partner RLS policies allow team members to manage data. Phases 5/5b/6 made team members able to LOG IN, but the data-layer RLS still refused them the moment they tried to INSERT/UPDATE/DELETE. The four partner-data policies (`partner_students`, `partner_applications`, `partner_leads`, `partner_fees`) all had `qual` clauses that referenced `partners.user_id = auth.uid()` — the org-owner path. A team member has a `partner_team_members` row but no `partners` row, so every write op hit "new row violates row-level security policy". Caught by end-to-end testing — the team member could log in, see their partner org, hit GET endpoints fine, but POST crashed at the DB layer. Fix: new SECURITY DEFINER helper functions `is_partner_member(partner_id uuid)`, `is_partner_owner(partner_id uuid)`, `is_partner_team_member(partner_id uuid)` in the public schema. SECURITY DEFINER runs with the function owner's permissions, which bypass RLS on `partner_team_members` — necessary because a direct subquery from one RLS policy into another table that has its own self-referencing RLS triggers "infinite recursion detected" (v1 of this migration hit that, rolled back, v2 uses the helpers). 4 RLS policies rewritten to use `is_partner_member(partner_id)` in both USING and WITH CHECK — matches if caller is the org owner OR an active team member; new rows must have a partner_id the caller is bound to. Live tested: create team member via direct-create → POST student/app/lead all → 201 with partnerId bound to the owner's partner_id; PATCH student → 200; list as member shows only own rows (Phase 3 scoping intact); list as owner shows all; new student row's partner_id in DB matches owner's (verified via direct SQL); `/api/partner/team` still owner-only for member role. `pnpm ts-check`: 0 errors.
- **Phase 8** (2026-06-08): Owner can reset team member's password. The team page had Remove but no recovery path for a member who forgot their password — the member couldn't trigger Supabase's password recovery email (no admin dashboard access), and the recovery email would land in their own inbox only if they happened to know the right address. Owner had to delete + recreate the member (losing metadata) to recover. New `POST /api/partner/team/[id]/reset-password`: owner-only via `requireTeamMember`+`role==='owner'`; verifies the team_member's partner_id matches the caller's (blocks cross-partner attacks); refuses to reset the owner's own password (chicken-and-egg lockout — owner should use Supabase's recovery flow for themselves); refuses to reset pending_invite members (no password to reset yet); applies the new password via `service.auth.admin.updateUserById(uid, { password })`; returns the email + new password so the UI can show it once. Min 8 chars. UI: "Reset password" KeyRound icon button on every active member row; modal with form (password input + Generate button using `crypto.getRandomValues` with 14-char strong default) and result (email + password with Copy buttons, emerald success banner). Reuses the `copyToClipboard` helper from Phase 5. 12 new i18n keys (en + zh) under `partnerTeam.*`. Live tested: reset via API → new password works, old rejected; cross-partner attack → 404; owner tries to reset own → 400 with helpful message; password < 8 → 400; `/partner/team` page → 200. `pnpm ts-check`: 0 errors.
- **Phase 9 (bugfix)** (2026-06-08): Team member hits JSON errors in partner layout + team page. When a team member signed in and navigated the partner portal, two surfaces were showing raw JSON 403 errors as red banners instead of friendly messages. (1) `/api/partner/login-status` returned 403 for team members because it only looked up via `partners.user_id = auth.uid()` — the org-owner path. Team members 403'd. The partner layout calls this on every page load to decide which shell to show + the status, so the 403 crashed the whole layout for team members. (2) `/partner/team` page rendered a 403 "Only owners can manage the team" from the API as a red error banner — the page was trying to fetch the owner-only team list. The sidebar correctly hid the link for non-owners via `ownerOnly: true`, but a non-owner who landed on the URL directly (bookmark, typed it in) saw the JSON error before the page redirected them. Fix: `/api/partner/login-status` rewritten to use the same two-path lookup as `requireTeamMember` / `requirePartner` / `/api/partner/me` from Phase 5b — try org-owner first, fall back to team-member via `partner_team_members.user_id` joined to `partners` via `partner_id`. `/partner/team` page pre-checks the role via `/api/partner/login-status` before fetching the team list — if the caller isn't an owner, renders a friendly "Owner-only section" message (UserCog icon) instead of trying the API and showing the 403. 2 new i18n keys (en + zh). Live tested: login-status as member → 200 with role='member'; as owner → 200 with role='owner' (regression); full partner API smoke shows the right 200/403 mix for member role. `pnpm ts-check`: 0 errors.
- **Phase 10 (bugfix + hardening)** (2026-06-08): Root-caused the PGRST116 "JSON object requested, multiple (or no) rows returned" error that was haunting the team member login path. Diagnosed: ziovomart's user (5e224dda...) had **two** `partner_team_members` rows — one as a member of Ednex from the email-invite flow (role='member', status='active', partner_id=b7250a7b Ednex) and one as owner of their own org from my Phase 6 direct-DB recovery insert (role='owner', status='active', partner_id=f3103c0f Ziovomart). The two-path lookup in `requireTeamMember` / `requirePartner` / `/api/partner/me` (Phase 5b) and `login-status` (Phase 9) uses `.single()`, and `.single()` returns 0 rows / >1 rows as PGRST116 → 403 "No partner account bound to your user" on every API call. **Cleanup**: deleted the Ednex row (13e6c778); ziovomart now has exactly 1 team_members row. Live-verified: `GET /api/partner/me` → 200 with the partner org; `GET /api/partner/login-status` → 200; the recovery flow from Phase 6 works. **Hardening** (defense in depth so the next half-signup can't reintroduce the same bug): (1) `/api/partner/signup` route now also checks `partner_team_members.user_id` after the existing `partners.user_id` check — if the user has a stray team_members row (e.g. orphan from a deleted partner org, or a previous direct-DB recovery), the route refuses with 409 + `code: 'TEAM_MEMBERSHIP_EXISTS'` instead of silently creating a second team_members row. (2) Migration `database/2026-06-08_partner_team_members_user_id_unique.sql` adds a hard `UNIQUE (user_id)` constraint to `partner_team_members` at the DB level. The existing unique constraint was only on `(partner_id, user_id)`, which allowed a user to be in multiple partner orgs. The design (enforced by `/api/partner/team` POST's 409 message and Phase 5b's two-path lookup) is "one user belongs to one partner org" — this finally hardens the DB to match. The migration includes a `DO $$` block that raises if any dupe rows exist, so applying the constraint on dirty data fails loudly instead of corrupting state. Live-verified: pre-migration `COUNT(*) = 5, COUNT(DISTINCT user_id) = 5` (clean); test-insert of a duplicate row → Postgres 23505 unique_violation; signup/complete-setup/team-invite/create-with-password all four partner_team_members INSERT sites have the user_id idempotency check. The new 4 INSERT sites are now: team POST email-invite, team/create-with-password POST, signup POST, complete-setup POST. `pnpm ts-check`: 0 errors.
- **Phase 11 (team management ops)** (2026-06-08): Two operational gaps in partner team management that real partner orgs will hit within months. **(1) Suspend / Reactivate**: the schema already had `status='suspended' + suspended_at + suspension_reason` (RLS helper `is_partner_team_member` filters on `status='active'`, so the data layer was already correctly excluding suspended members), and the team page was already reading the columns, but **there was no API to flip a member to/from suspended** — every doc stayed `active` forever and Remove was the only way to kick someone out. New `POST /api/partner/team/[id]/suspend` (owner-only, optional `reason` body, refuses self-suspend + suspend-pending_invite + suspend-last-active-owner, idempotent on already-suspended). New `POST /api/partner/team/[id]/reactivate` (owner-only, refuses reactivate-self + reactivate-non-suspended, clears `suspended_at` + `suspension_reason`). **(2) Ownership transfer**: the schema allows `role='owner'` on multiple members, the DELETE route refused to remove the last active owner, but **there was no way to add a second owner or hand off to a member** — the owner could never step down without admin help. New `POST /api/partner/team/[id]/transfer-ownership` (owner-only, `[id]` is the target member; the API demotes the caller to `member` and promotes the target to `owner` via two sequential UPDATEs with a best-effort rollback on the second failure; refuses self-target, target-not-active, target-already-owner). After a transfer, the layout's `/api/partner/login-status` re-check hides the team sidebar item from the (now-demoted) caller. **(3) Hardening the lockout**: the initial implementation correctly blocked the suspended member on `/api/partner/students + /applications` (they use `requireTeamMember` which already 403'd on `status !== 'active'`), **but** `/api/partner/me` + `/api/partner/login-status` rolled their own two-path lookup and returned 200 with the partner data — making the suspend button cosmetic. Fix: `requirePartner` (the looser helper used by `/me` + `/login-status` + `/students` + `/applications` + `/notifications`) now mirrors the `requireTeamMember` status check and returns 403 with `"Your team membership is suspended. Please contact your partner owner."` for non-active members. `/api/partner/me` + `/api/partner/login-status` now return 200 with `teamMember: {id, role, status}` so the layout can detect a personal suspension (vs an org-level one) and show a team-membership-specific message. New `partnerLayout.teamMemberSuspendedMessage` i18n key (en + zh). **(4) PostgREST column-silently-missing bug fix**: the first version of `transfer-ownership` selected `'id, role, status, user_id, email'` from `partner_team_members` — `email` is not a column on that table (it lives in `auth.users` and the team GET hydrates it separately). PostgREST silently returns 0 rows when you ask for a non-existent column, so the endpoint 404'd on every valid request. Removed `email` from the SELECT. The same bug was already absent from the suspend/reactivate routes (which only selected `id, role, status, user_id`). **(5) UI**: `/partner/team` page grew from 918 → 1133 lines. Three new modals: **Suspend** (orange alert + reason textarea, 500 char max), **Reactivate** (green confirm with `RotateCcw` icon, no fields), **Transfer Ownership** (amber warning + select dropdown of `activeMembersExceptMe`, explicit "you will become a regular member" copy, success banner shows the new owner's email). New buttons on member rows: orange "Suspend" between Reset Password and Remove on active rows; green "Reactivate" + red "Remove" on suspended rows; blue "Transfer ownership" on the owner's own row (only when there's at least one other active member to transfer to). Per-row inline success badges: ✓ Suspended / ✓ Reactivated / ✓ Ownership transferred (auto-clear 4-5s). 25 new i18n keys (en + zh) under `partnerTeam.*`. **(6) Test fixes** (carryover from the pre-existing i18n drift + partner-student-mapper failures): added zh translations for 10 S40 footer keys that were en-only; added zh translations for `partnerStudentNew.bannerNotes + bannerBodyEnd` (the en version italicizes "Notes" inline; zh was missing the split keys); removed a stray zh-only `partnerStudents.fieldPhonePlaceholder` that should never have been there (the en equivalent is `partnerStudentNew.fieldPhonePlaceholder`); updated the partner-student-mapper test to expect the new `createdByUserId + createdByEmail` fields the mapper has been returning for a while. `pnpm ts-check`: 0 errors. `pnpm test`: 12 test files, 111/111 passing (was 2 failing). Live-tested end-to-end: create member → suspend → member's `/me + /login-status` return 200 with `teamMember.status='suspended'`; `/students + /applications + /notifications` 403; reactivate → all 200 again; transfer ownership → roles swap, original owner becomes member, new owner sees the team nav; reverse the transfer for cleanup. Edge cases verified: self-suspend 400, self-transfer 400, reactivate-self 400, reactivate-non-suspended 400, re-suspend idempotent (`alreadySuspended: true`).
- **Phase 12 (listUsers → getUserById/getUserByEmail + Last seen)** (2026-06-08): Three partner endpoints (`/api/partner/team` GET, `/api/partner/team` POST, `/api/partner/team/create-with-password` POST, `/api/partner/students` GET) all used `auth.admin.listUsers({ perPage: 200 })` to hydrate auth metadata. Two real problems: (1) **silently truncated at 201+ users** — an email at the bottom of the alphabet was just "not found", no error surfaced (caught the owner invitation flow's silent failure); (2) **privacy** — the partner server held every user in the project in memory even though the partner only needed their own 5-10 team members. New `src/lib/partner-user-lookup.ts` with two functions: `hydrateUserEmails(supabase, userIds[])` does **parallel `auth.admin.getUserById(id)`** calls (one per user, with a 60s in-memory cache to avoid hammering GoTrue on every page load); `findUserIdByEmail(supabase, email)` does a **paginated `listUsers({perPage:200, page})` scan** that stops on first match (capped at 10 pages = 2000 users) because the Supabase JS admin API (2.95.x) doesn't expose `getUserByEmail` and the underlying GoTrue `?email=` filter is ignored. Plus a `primeEmailToUserIdCache()` helper for createUser-success: without it, a follow-up invite with the same email within the 60s TTL would hit the stale "not found" cache entry and try to createUser again — Supabase rejects with 500 "A user with this email address has already been registered" instead of cleanly 409. The four endpoints now use the helper; the team GET also surfaces `last_sign_in_at` as `lastSignInAt` (free, since we already do per-user lookups) and the team page renders **"Last seen: 3d ago"** in the row meta line via a new `formatRelativeTime()` helper (just now / Nm ago / Nh ago / Nd ago / absolute date). 5 new i18n keys (en + zh) under `partnerTeam.{lastSeen,relativeJustNow,relativeMinutesAgo,relativeHoursAgo,relativeDaysAgo}`. Live-verified: team GET returns email + lastSignInAt; students GET still hydrates `createdByEmail`; create-with-password then re-invite within 60s returns 409 (not 500) thanks to `primeEmailToUserIdCache`. `pnpm ts-check`: 0 errors. `pnpm test`: 12/111 passing.
- **Phase 13** (2026-06-08): Partner dashboard `limit=100` silently capped the "Total Students" card. The page was calling `students.length` from a paginated response capped at 100 rows — a partner org with 150 students saw "100". Both `/api/partner/students` and `/api/partner/applications` already return `total` from Supabase's `count: 'exact'`, so the fix is to read `total` for the headline numbers. Status breakdowns (active / accepted) still derive from the fetched slice — adding a real aggregate would mean a new endpoint or multiple `?status=` round trips the API doesn't support, not worth it for a dashboard card. A small amber hint (`partnerDashboard.statusBreakdownCapped`) flags the active/accepted cards as lower-bound estimates when the org has more than 100 rows. 2 new i18n keys (en + zh). `pnpm ts-check`: 0 errors. `pnpm test`: 12/12 files, 111/111 passing.
- **Phase 14** (2026-06-08): Rate-limit the two partner team-creation endpoints. `POST /api/partner/team` (email-invite) and `POST /api/partner/team/create-with-password` had no abuse guard — an owner could loop and burn the Resend quota (email-invite) or spam the auth.users table (direct-create). New `src/lib/rate-limit.ts`: tiny in-memory sliding-window limiter (Map of `hits[]` per `action+key` bucket), 5 hits per 15 minutes per owner, returns 429 with `Retry-After` header when exceeded. **Caveat**: process-local, resets on deploy/restart, won't stop a multi-replica attacker — covers the realistic abuse-from-inside case. Applied at both endpoints after the owner role check, before any side effects (no DB writes or emails sent on 429). 4 new vitest cases (allow up to max, block at max+1, bucket isolation by action+key, sliding-window drop-off). `pnpm ts-check`: 0 errors. `pnpm test`: 13/13 files, 115/115 passing (4 new).
- **Phase 15** (2026-06-08): "Team Members" stat card on `/partner` dashboard. The 3-card dashboard (Total Students / Active Applications / Accepted) never showed the org's team size — owners managing a growing team had no at-a-glance signal. New 4th card with total member count + inline `{{count}} active · {{count}} pending invite` sub-line + "Manage Team" button. The team GET is owner-only (403 for non-owners), so the fetch is wrapped in a thenable that resolves to a tagged union — a 403 (or any team fetch failure) is swallowed and the card simply doesn't render. Card render gated on `team !== null && teamLoaded` to avoid a flash on first paint. 5 new i18n keys (en + zh) under `partnerDashboard.{teamMembers, teamMembersDesc, teamMembersActiveInline, teamMembersPendingInline, viewTeam}`. `pnpm ts-check`: 0 errors. `pnpm test`: 13/13 files, 115/115 passing. Live-verified `/partner` renders HTTP 200.
- **Phase 16** (2026-06-08): Show company name (not user/email) as the partner org identifier. The sidebar footer primary line was `partner.contact_person || partner.email`, the top bar was just `partner.email`, and the team page header was bare "Team" with no org context. Fixed all three: sidebar primary = `company_name` (fallback email), sub-line = `contact_person · email`, avatar initial = company name's first letter; top bar = `company_name`; team page header = `Team · {company_name}` (fetches `/api/partner/me` on mount, fails soft). An owner with multiple partner orgs in different tabs now sees distinct sidebars; a team member landing on the team page can see which org they belong to. `pnpm ts-check`: 0 errors. `pnpm test`: 13/13 files, 115/115 passing.
- **Phase 17** (2026-06-08): Partner team members couldn't be restricted from changing company name or seeing the commission rate. Two real gaps: (1) `PATCH /api/partner/me` used `.eq('user_id', auth.user.id)` which was safe-by-coincidence for team members (no `partners` row → 0-row update → 404), but a 404 with a misleading message isn't a real guard; (2) commission_rate (admin-set financial info — the org's % cut per successful student application) was returned to anyone who hit `/api/partner/me`, then rendered on the Rates tab. Fixed both. PATCH: explicit owner probe at the top, returns 403 "Only the org owner can edit organization details" for non-owners. GET: strips `commission_rate` + `status` from the response for the team-member path; the response now includes `viewerRole: 'owner' | 'member'` so the UI can branch without a second round-trip. UI: Company Name + Contact Person inputs go disabled for team members, save button replaced with "Read-only for team members" italic hint; Rates tab nav button + body gated on `isOwner`; the page defaults `viewerRole` to 'member' (safe until the /me response confirms). 3 new i18n keys (en + zh) under `partnerSettings.{sectionCompanyInfoDescReadOnly, ownerOnlyEditHint, errorOwnerOnly}`. `pnpm ts-check`: 0 errors. `pnpm test`: 13/13 files, 115/115 passing. Live: `/partner/settings` HTTP 200; PATCH/GET with no auth → 401.
- **Phase 18** (2026-06-08): Fixed 4 critical bugs surfaced by the add-application audit. (C1) `auth.admin.listUsers({perPage: 200})` regression in `/api/partner/applications` GET — Phase 12 explicitly replaced this pattern with `hydrateUserEmails` in 4 endpoints and missed this one. Silently truncated `created_by_email` at 201+ users. Switched to the parallel `getUserById` + 60s cache helper. (C3) Admin "create application" wizard — lead path was unreachable. Step 1 has a Select for existing students AND an "OR — NEW LEAD" section, but the Next + Submit buttons were disabled when `!formData.studentId`, blocking the lead path entirely. Fixed: allow either studentId OR a valid lead (applicantName + applicantEmail both filled). (C4) Admin "create application" — `notes` field was sent as BOTH `additionalNotes` (student-visible) AND `adminNotes` (admin-only). The form has one Textarea, so the student would see whatever the admin typed. Fixed: only send `additionalNotes` on create; admin can write admin-only notes from the detail page where the fields are properly separated. (C2) Admin "create application" — Documents to Sync was a silent no-op. The wizard has a full "Available Documents" panel with checkboxes + "Auto-sync" toggle + "N of M selected" counter + a review step showing green badges, but `selectedDocuments` was never sent in the POST payload. Fixed: extended `/api/admin/documents/[id]` PATCH to accept an optional `applicationId` field (status is now optional too, so a link-only PATCH doesn't implicitly auto-verify a Pending doc); admin new-app `handleSubmit` now PATCHes each selected doc with just `{applicationId}` after the application is created, via `Promise.allSettled` (single failure doesn't block the rest). `pnpm ts-check`: 0 errors. `pnpm test`: 13/13 files, 115/115 passing.
- **Phase 19** (2026-06-08): Add-application validation cleanup (H1-H5 from the audit). (H1) Student POST had exact duplicate validation (lines 94-99 then 100-105 were copy-pasted `!trimmedDegree && !isDraft` and `!trimmedIntake && !isDraft` checks) — dead code, removed. (H2) Student POST imported `parseApplicationStatus` but never used it — the workaround was a `void parseApplicationStatus` hack to silence the linter. Dropped the import. (H3) Admin POST had no `degree` enum validation while student + partner both enforce the closed taxonomy; added a 400 with the allowed list. (H4) Admin POST had no `priority` enum validation; same fix. Both H3/H4 reuse the existing `PARTNER_APPLICATION_DEGREES` / `PARTNER_APPLICATION_PRIORITIES` constants from `partner-application-mapper.ts` (single source of truth, no copy). (H5) Verified — partner mapper already validates both degree and priority (lines 477-480 + 487 of `partner-application-mapper.ts`); no code change needed. `pnpm ts-check`: 0 errors. `pnpm test`: 13/13 files, 115/115 passing.
- **Phase 20** (2026-06-08): Admin "create application" wizard now uses the live catalog (H6 from the audit). The page imported `universities` + `programs` from the static `data.ts` fallback — same root cause as the Phase 3 chatbot fix and Phase S20 student-wizard refactor. Admin-added schools + programs were invisible in the wizard. Now fetches `/api/universities?limit=500` + `/api/programs?limit=500` in parallel on mount; the Program `<Select>` shows a "Loading catalog..." placeholder while the fetch is in flight and an inline "No programs found for this degree level." empty state when the filter yields no matches. `degreeLevels` / `intendedIntakes` / `documentTypes` stay as static imports — closed taxonomies not yet admin-managed in the UI. `pnpm ts-check`: 0 errors. `pnpm test`: 13/13 files, 115/115 passing. Live: `/admin/applications/new` HTTP 200.
- **Phase 21** (2026-06-08): Typeahead student picker (H7 from the audit). The admin "create application" wizard had a plain `<Select>` for "Select Existing Student" capped at 100 rows by `useStudentList` — for a 200+ student org, anyone whose name sorted below row 100 was unreachable. Two changes: (1) `src/hooks/use-student-list.ts` cap raised from 100 → 500 via a new `STUDENT_LIST_LIMIT` constant — 500 is a pragmatic ceiling that covers the realistic 200+ case; any real production org at 500+ should move to a server-side search endpoint (out of scope). (2) The plain `<Select>` in the wizard swapped for `<SearchableSelect>` (the same component the student wizard + partner new-app form use); type-to-filter on name / email / nationality via cmdk. Sub-line in each option is `email · nationality` for disambiguation. Loading state wired up via `useStudentList`'s `isLoading` flag. `pnpm ts-check`: 0 errors. `pnpm test`: 13/13 files, 115/115 passing. Live: `/admin/applications/new` HTTP 200.
- **Phase 22** (2026-06-09): Six add-application polish items from the audit. `admin/applications/new/page.tsx`: (H8) inline amber warning when admin has BOTH a student selected AND lead fields filled (API rejects with 400; before, the conflict only surfaced at submit time). (H9) status was hardcoded `'Submitted'` — new "Initial Status" `<Select>` on step 2 with Draft | Submitted (default Submitted preserves existing behavior; admins can now adopt Draft at intake). (M1) priority was always `'Medium'` (server default) — new "Priority" `<Select>` on step 2 (Low | Normal | High | Urgent, default Normal) so admins can triage at intake. (M2) real bug — `personalStatement` was in formData and in the POST payload but the form had no `<Textarea>` for it, so the field was always empty on create; added a 5-row `<Textarea>` with 4000-char limit + N/4000 counter; surfaces in step 3 Review. (M7) header "Back" button used `router.back()` — through a step-wizard that lands on whatever browser history says (`/admin/students`, `/admin`, anywhere); switched to `router.push('/admin/applications')` for consistency with post-submit nav. (Bonus M3) added `maxLength=2000` + N/2000 counter to Additional Notes. `student/applications/new/page.tsx`: (M8) dead state cleanup — `pendingDocIds` was initialized, written to in 3 places, and cleared, but never read; replaced by Phase S25's `linkOrphanDocsToApplication` (re-fetches the full orphan list after POST and links everything in parallel); removed the useState, both `setPendingDocIds([])` clears, and the `setPendingDocIds((prev) => ...)` call in onUploaded. `pnpm ts-check`: 0 errors. `pnpm test`: 13/13 files, 115/115 passing. Live: `/admin/applications/new` HTTP 200.
- **Phase 23** (2026-06-09): Last partner-form polish (M4/M9/L4) from the audit. (M5 from the audit was already done — the PartnerApplicationForm has had a Priority `<Select>` with default 'Normal' in `INITIAL_FORM_DATA` for a while; the audit's M5 description was stale.) (M4) optional `applicationNumber` input in the Workflow section next to Priority — for partners with internal CRM IDs they want to track alongside SICA's auto-minted `PA-YYYY-NNNN`. Whitespace-only is treated as unset → server mints a fresh one. Partner's CRM ID shows on the detail page + exports alongside the SICA number. (M9) `studentEmail` format validation — the field is optional but if filled must be a real format; `type="email"` gives a soft browser cue but doesn't block submit, so added a regex check in the per-field validation block. Trims before checking so a stray space doesn't look valid. Real bug fix — a typo'd `gmial.com` used to silently store and bounce when SICA emailed the student. (L4) success toast on create — was jumping straight to the detail page with no feedback. New `submitSuccess` state + green banner with `CheckCircle` icon + 1500ms delay before navigation. Copy explains "Application created as Draft. SICA's admin team will review and update the status." (Draft wording because partner rows always start as Draft per S27.) 4 new i18n keys en + zh. `pnpm ts-check`: 0 errors. `pnpm test`: 13/13 files, 115/115 passing. Live: `/partner/applications/new` HTTP 200. Skipped (called out, not fixed): M6 (student-side priority — design call: admins and partners drive priority, students don't self-prioritize), L1 (3 different create-app visual styles — redesign too big for polish), L2 (admin i18n — deferred per roadmap), L3 (personal statement draft copy — very minor).
- **Phase 24** (2026-06-09): Fixed 4 broken CTAs on `/universities/[slug]` (the highest-intent page in the funnel). (C1) The SICA Support Card's "Apply" `<Button>` was a bare button with no `<Link>` wrapper, no `onClick`, no `href` — clicks did nothing on the page most likely to convert. Wrapped in `<Link href={\`/contact?interest=${slug}\`}>` so the lead lands on the contact form with the university in the URL. (C2) Three `href="#"` placeholders in the right-rail cards: "View All Programs" → `/programs?university=<slug>` (programs filter from Phase 1.5 already supports this param), "View Fees" → removed (no detail section exists, the card already shows undergrad + grad tuition inline), "View Admissions" → `/contact?interest=<slug>` (natural action after reading intake info is to ask a counselor about timing). `pnpm ts-check`: 0 errors. `pnpm test`: 13/13 files, 115/115 passing. Live: `/universities/tsinghua-university` HTTP 200.
- **Phase 25** (2026-06-09): Home CTA redirect tuning (H1 + H2 from the funnel audit). (H1) Hero secondary "How to Apply" was `/universities` — the button label is a question, not a browse action, so pointed it at `/guides/application` which answers the question with the actual process (documents, timeline, fees) at zero commitment. (H2) Bottom "Apply" was `/universities` — this is the deepest-intent slot on the home (user has scrolled past the hero, why-SICA, featured schools, testimonials, news, then is asked "ready to take the next step?") so pointed it at `/assessment`, the structured 4-step intake that captures goal + background + budget. The home now has 3 distinct entry points at 3 different commitment levels (`/universities` = browse, `/guides/application` = learn, `/assessment` = commit) instead of 2 entries to the same browse page. The `/contact` mailto stays as the softer secondary for users who want to talk first. `pnpm ts-check`: 0 errors. `pnpm test`: 13/13 files, 115/115 passing. Live: `/`, `/guides/application`, `/assessment` all HTTP 200.
- **Phase 26** (2026-06-09): UTM + click-id capture on lead forms (H3 from the funnel audit). Contact form and assessment form were capturing `sourcePage` (pathname) only — neither read `?utm_source=...` etc. from the URL. Added the 3 essential UTMs (source / medium / campaign) + `gclid` + `fbclid` with `sessionStorage` persistence so cross-page navigation doesn't lose attribution. New `src/lib/utm.ts` helper with whitelist enforcement (only the 7 named params are captured; anything else in the URL is dropped to prevent a hostile URL from stuffing arbitrary keys). 7 files changed: new `src/lib/utm.ts` + 9-case vitest suite, both forms call `getCurrentUtm()` on submit, both API routes whitelist the new fields and pass them through, new `database/2026-06-09_utm_attribution.sql` migration adds 5 columns + 4 partial indexes to `contact_submissions` and `student_assessments`. `pnpm ts-check`: 0 errors. `pnpm test`: 14/14 files, 124/124 tests passing (9 new). **Migration applied via Supabase dashboard SQL editor by user on 2026-06-09 — confirmed via PostgREST introspection (5 new columns present, all 4 partial indexes created). End-to-end smoke test: POST `/api/leads` with all 5 UTM fields round-tripped through the API into the DB correctly (`utm_source=google`, `utm_medium=cpc`, `utm_campaign=phase26_smoke_2026`, `gclid=TeSter-...`, `fbclid=TeSter-...`); cleanup 204.** Live: `/contact` + `/assessment` HTTP 200 with and without UTM query params.
- **Phase 27** (2026-06-09): `/thank-you` page — the biggest single conversion-rate lift in the funnel audit (H5). Before: both forms showed an inline "Thanks, we'll be in touch" message and stayed there — the lead's last impression was the form, not the brand. After: both forms now `router.push('/thank-you?...')` after a 250ms delay. The page is personalized by `?source=` (contact | assessment — affects the hero title + body copy) and `?interest=` (university slug from the Apply CTA redirect chain wired in Phase 24 — surfaces a "you were looking at this" personalisation card). 7 sections: hero with check-mark + client-generated reference number (`SICA-YYYYMMDD-XXXX` format the lead can quote if they call us), 3-step timeline ("what happens next"), multi-channel reach-out (WhatsApp with pre-filled context-aware message, Email, Schedule-a-call), 3 social-proof stats (24h response, 92% placement, 27+ partner unis), "while you wait" related content, 3-question FAQ as `<details>` accordions, share + back-home with copy-link (Web Clipboard API + textarea fallback). Server component wrapping a client island in `<Suspense>` (required by Next.js for `useSearchParams`); Suspense fallback is a static skeleton mirroring the hero so no flash. 40 new i18n keys (en + zh) under `thankYou.*` namespace. 5 files changed: 2 new (page.tsx + ThankYouClient.tsx), 2 form files (redirect on success), 1 i18n. `pnpm ts-check`: 0 errors. `pnpm test`: 14/14 files, 124/124 passing. Live: `/thank-you`, `/thank-you?source=contact`, `/thank-you?source=assessment&interest=tsinghua-university` all HTTP 200.
- **Phase 28** (2026-06-09): Sticky "Apply" CTA on long university pages (H6 from funnel audit). `/universities/[slug]` is 1,123 lines — once the user scrolls past the hero the primary "Apply" CTA is gone for the rest of the page. New `src/components/StickyApplyBar.tsx` (client component, 194 lines) — appears once `scrollY > max(50vh, 400px)` (whichever is greater, so it shows up after the hero on tall screens and not-too-far-down on short ones), slide animation via `translate-y-full` ↔ `translate-y-0` (300ms ease-out), dismiss button (X, left side) hides for the rest of the session for this specific university (sessionStorage keyed by slug so a different universidad in a different tab still gets its own bar), 2 actions: WhatsApp (pre-fills "Hi SICA, I'd like to apply to <name>. Can you help me?") + Apply (routes to `/contact?interest=<slug>` via Phase 24's chain so the contact form pre-fills the university). Mobile: full-width bar with just the close + 2 buttons; desktop: `max-w-3xl` container with the school name + "Applying to" label. Brand: `bg-[#1B2A4A]` deep dark blue with `border-t-4 border-[#9B1B30]` crimson top border, Apply button crimson, WhatsApp `#25D366` standard wa-green. Mounted on `/universities/[slug]` after the related-news section, before the main return's closing `</>` — the bar only renders once `uni` is populated (the page is a client component that fetches on mount), so its `universityName` + `universitySlug` props are always valid. 5 new i18n keys (`stickyApply.*` namespace) in en + zh: `ariaLabel`, `dismissLabel`, `applyingTo`, `whatsapp`, `apply`. 3 files changed: 1 new (component), 1 mount (page), 1 i18n. `pnpm ts-check`: 0 errors. `pnpm test`: 14/14 files, 124/124 passing. Verified the bar's `sica_sticky_apply_dismissed_<slug>` sessionStorage key is in the client bundle (`src_10600aaa._.js`), so the component code is actually shipped, not just imported. Live: `/universities/tsinghua-university` HTTP 200.
- **Phase 28b** (2026-06-09): Sticky bar + Support Card Apply both route to `/assessment?interest=<slug>`, not `/contact?interest=<slug>`. Phase 25's intent gradient put `/assessment` at the commit-level slot and `/contact` at the softer "talk to a counselor" path — but both primary Apply CTAs on the university detail page were using the softer path. The `/contact` route is preserved via the right-rail "Or talk to a counselor" link right below the Support Card's primary Apply button. The assessment form already reads `?interest` from `useSearchParams` (line 53) and passes it through to `/thank-you?source=assessment&interest=<slug>`, so the "you were looking at this" personalized card on the thank-you page still lights up. 2 files changed. `pnpm ts-check`: 0 errors. `pnpm test`: 14/14, 124/124 passing. Live: `/universities/tsinghua-university` → 200.
- **Phase 29** (2026-06-09): Google Analytics 4 wiring. `@next/third-parties/google@16.2.7` (Vercel's official Next.js wrapper, exported as a subpath of `@next/third-parties` which is the actual installed package) — handles SPA page-view tracking on App Router route changes automatically, so no manual usePathname + useSearchParams listener. New `src/lib/analytics.ts` is a strongly-typed wrapper around `sendGAEvent` with a 13-event `AnalyticsEventMap` (`apply_click`, `whatsapp_click`, `assessment_start` / `_step_complete` / `_submit`, `contact_submit`, `university_click` / `program_click` / `scholarship_click`, `search_performed`, `filter_change`, `chatbot_message_sent` / `_opened` / `_closed`) — every event requires `locale: 'en' | 'zh'` as a property so the user can segment by language. SSR-safe (no-op on server), call site gets autocomplete + compile error on typo. `<GoogleAnalytics gaId={...}>` in `src/app/layout.tsx` is env-gated on `NEXT_PUBLIC_GA_MEASUREMENT_ID` — unset = no script fires (safe default; live verified with 0 gtag hits in the rendered HTML). Consent mode v2: `analytics_storage: 'granted'` by default (no consent banner — SICA's audience is mostly outside EU/China-strict, marketing-funnel UX). TODO hook in the helper's JSDoc for a future consent banner. Event wiring: `StickyApplyBar` (apply_click + whatsapp_click), `universities/[slug]` Support Card (apply_click), `whatsapp-float` (whatsapp_click), `assessment-form` (start on mount, step_complete on Next, submit on 200), `contact-form` (submit on 200), `universities` / `programs` / `scholarships` list pages (card click + position), `universities` search box (600ms debounce), `Chatbot` (open/close), `ChatWindow` (message_sent with length). `.env.example` + `.env` + `package.json` updated. `pnpm ts-check`: 0 errors. `pnpm test`: 14/14, 124/124 passing. `pnpm build`: succeeds. Live: `/`, `/universities`, `/assessment`, `/contact` → 200; no GA script in HTML (env var empty). To go live: set `NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX` in Vercel/Railway env and redeploy.
- **Phase 30** (2026-06-12): Partner portal — student document management (Approach B). Partner staff (org owner OR active team member) can upload, list, edit, download, bulk-move, and delete documents they manage on behalf of `partner_students`, reusing the existing `student_documents` table so admin reviewers see partner docs in the same queue as student-uploaded docs. **Data layer (Q1a/Q2a/Q3a decided)**: `database/2026-06-12_partner_documents.sql` adds `student_documents.partner_student_id` (nullable uuid, FK → `partner_students(id)` ON DELETE CASCADE) + `student_documents.partner_application_id` (nullable uuid, FK → `partner_applications(id)` ON DELETE SET NULL, free-win for review context beyond the brief) + 2 partial indexes; new `SECURITY DEFINER STABLE` helper `public.is_doc_partner_member(p_partner_student_id uuid)` joins through `partner_students.partner_id` into the Phase 5b `is_partner_member` (avoids the partner_team_members → RLS self-recursion trap from Phase 5b v1) + 4 new RLS policies on `student_documents` (Partner team can view/insert/update/delete their own documents) gated on `partner_student_id IS NOT NULL AND is_doc_partner_member(partner_student_id)`. Existing admin policies untouched (they gate on `is_admin()` only, so partner rows with `student_id=NULL` are still visible in the admin review queue). 3 new storage helpers in `src/lib/storage.ts` (lines 182-288) reuse the existing `STUDENT_DOCS_BUCKET` + `STUDENT_DOC_ALLOWED_TYPES` + `STUDENT_DOC_MAX_BYTES` — no new bucket, no new constants; namespace is `partner/{partnerId}/{partnerStudentId}/{docId}-{safeName}.{ext}`. **API layer**: 7 new routes under `/api/partner/documents/` (list+create `/route.ts`, `upload-url/route.ts`, `[id]/route.ts` GET+PATCH+DELETE, `[id]/download-url/route.ts`, `bulk/route.ts`) all gated on `requireTeamMember` + carry an explicit cross-tenant prefix guard `fileUrl.startsWith('partner/${auth.partnerId}/')` (defense in depth, not just RLS) + DELETE cleans up the Storage object (intentional per scoping memo §2f, documented in the dialog JSDoc so future-him doesn't "fix" it back to orphan-only). New closed-set mapper `src/lib/partner-doc-mapper.ts` (6 `PARTNER_DOC_CATEGORIES` + 3 `PARTNER_DOC_STATUSES` + `mapPartnerDocumentFromDb` camelCase coercion with safe fallbacks to `'Other'`/`'Pending'`); same 6 categories + same 3 statuses as student docs so admin review works without translation. The existing admin `PATCH /api/admin/documents/[id]` was extended (line 161 widens SELECT, then a new best-effort `partner_notifications` insert) so when an admin reviews a partner-uploaded doc with a status-bearing move to **Verified** or **Rejected**, a `partner_notifications` row is inserted with `type='document_review'`, `link_url='/partner/documents?applicationId=<id>'` (or `'/partner/documents'` when unlinked) — the existing `/partner/notifications` page from Phase 11 renders it automatically. **UI layer**: new `/partner/documents` page (1187 lines, `'use client'`, `useUrlState`-synced filters, sticky bulk bar, "Load more" pagination, 3-state sortable headers, active-filter pills, empty/loading/error states, reads `?applicationId=<id>` on mount to deep-link from the admin PATCH notification); 3 new dialogs (`DocumentUploadDialog` 666 lines, `DocumentEditDialog` 291 lines, `DocumentDeleteDialog` 156 lines — the delete dialog's copy explicitly says "this will also delete the file from storage"); 1 new sidebar nav item between Applications and Lead Sharing (`FolderOpen` icon, `partnerNav.documents` label); 102 new `partnerDocs.*` i18n keys in en + zh = 204 entries; 3 new partner-only GA events (`partner_documents_page_view`, `partner_document_upload`, `partner_document_delete`) added to the existing `AnalyticsEventMap` (chose single-map over separate `PartnerEventMap` for one `track()` overload + uniform `locale` property shape). **Cross-track LoC**: data-layer 146 sql + 108 ts = 254 lines; api-layer 8 files (7 routes + 1 mapper) = 1348 lines; ui-layer 4 files (page + 3 dialogs) = 2300 lines; i18n 204 entries; analytics 40 lines. **Test/build**: `pnpm ts-check` 0 errors, `pnpm test` 14/14 files 124/124 passing, `pnpm build` success (new `/partner/documents` page in route table), `pnpm lint:build` clean for every file I touched (the 2 `prefer-const` errors in `src/app/api/admin/partner-applications/route.ts` are pre-existing per the api-layer deliverable). Live smoke: dev server `GET /partner/documents` → 200. The admin PATCH deep-link `?applicationId=<id>` from a partner_notification click is consumed by the page (initial filter seed via `useRef` guard, then cleared from the URL). **Decisions**: (Q1a) `student_id = NULL` on every partner-uploaded row — student-side RLS gates on `student_id = auth.uid()`, so partner rows are invisible to students in their `/student/documents` view (no leak). (Q2a) `status = 'Pending'` on create — admin reviewer is the only path to Verified/Rejected. (Q3a) partner rows are in the same `student_documents` table — admin review queue unifies student + partner docs in one list, no parallel pipeline. **Open items for the user**: (1) live redeploy required (the live deploy is from the Phase 29 commit; `/partner/documents` won't be reachable until Railway picks up the new build). (2) the `partner_notifications.link_url='/partner/documents'` strings will work immediately because they're written server-side, not on render. (3) the brief allowed hard-coding the `link_url` string; the actual path matches.
- **Phase 31** (2026-06-12): Admin docs review queue — fix file URL + partner student name. Three real bugs surfaced by the Phase 30 partner-documents audit when the user clicked through a real partner-uploaded doc: (1) `/admin/documents/[id]` rendered `doc.file_url` directly as `<a href>`, `<img src>`, and `<iframe src>`. **file_url is a storage path** (e.g. `partner/abc/xyz/doc-123.pdf`), NOT a public URL — every "Open file" click 404'd. Affected BOTH student- and partner-uploaded docs (the bug was pre-existing for student docs; Phase 30 just made it visible because partner docs started landing in the queue too). (2) The Student card on the same detail page only joined `student_profiles` on `student_id`. Partner-uploaded docs have `student_id = NULL` and `partner_student_id = <uuid>`, so the card showed "—" and the only way to know who uploaded was the audit log. (3) Same bug on `/admin/documents` list — Student column showed "—" for every partner doc. **Fix (1)**: new `GET /api/admin/documents/[id]/download-url` endpoint mints a 1h signed URL via `createStudentDocDownloadUrl(fileUrl)` (same helper the partner side uses; reuses the bucket, no new constants). UI mints the URL in parallel with the doc fetch via `fetchSignedUrl` and uses it for the link, image, iframe, and "Open file" button. Renders a "Loading preview…" placeholder during the fetch + disables the open button if the signed URL is unavailable. **Fix (2)+(3)**: extended both admin GET endpoints to join `partner_students` via `partnerStudent:partner_students!partner_student_id (id, student_name, student_email)`. New `uploaderLabel()` helper in both pages resolves the non-null join (`doc.student` → "student" kind, `doc.partnerStudent` → "partner" kind, both null → "—") and the detail page swaps the Student card title to "Partner student" with a small hint that the partner gets the notification. The list page adds a small amber "Partner" badge next to the name so admins can disambiguate same-name students across portals at a glance. 4 new i18n keys × 2 languages = 8 entries (`adminDocs.detailPartnerStudent` + `detailPartnerStudentHint` + `columnUploaderPartner` × en/zh). **Bonus**: fixed 2 pre-existing `prefer-const` lint errors in `src/app/api/admin/partner-applications/route.ts` lines 105 + 127 (`let partnerNameById` / `let emailMap` → `const`) that had been blocking `pnpm lint:build`. **Why this is its own phase, not folded into Phase 30**: the URL bug was always present for student docs; it just took the partner-docs feature making a second class of docs visible to surface it. **Live**: commit `863d747` pushed; needs a Railway redeploy to reach `/admin/documents` with the fix. `pnpm ts-check` 0, `pnpm test` 14/14 124/124, `pnpm lint:build` 0, `pnpm build` success.
- **Phase 32** (2026-06-13): Admin application management — fix review flow + 3 real bugs. Five issues surfaced by a full review of the application-management surfaces (partner list/new/edit/detail, admin list/detail/partner-crm). **(1) Admin app detail status mutation was DEAD**: the page at `src/app/admin/applications/[id]/page.tsx` declared `'Approved'` as a status, but the DB enum + `PATCH /api/admin/applications/[id]` only accept `'Accepted'`. Approve/Reject/Withdraw buttons sent `Approved` → 400 from the API → silent error banner → status didn't change. Fixed: import `APPLICATION_STATUSES` from `src/lib/application-mapper.ts` (single source of truth), type the `confirmAction` state to a derived `AdminAction` union (`Accepted | Rejected | Withdrawn`). The mapper-import means a future enum rename can no longer drift this page silently — TS catches it. **(2) Unified admin list search ignored student name/email server-side**: the `?search=` filter on `/api/admin/applications` matched `program_name / university_name / application_number` only, but the page did a client-side `.includes()` on `studentName + studentEmail`. Result: search "John" returned 20 unrelated rows → JS filter narrowed to 0 → page looked empty. Fixed: added `applicant_name / applicant_email` to the student OR-filter and the joined `student.first_name / last_name / email` for linked apps. The partner query already searched `student_name + student_email`. **(3) Partner tab status filter didn't cross the student↔partner taxonomy**: student has `'Under Review'`, partner has `'In Review'` (same meaning). When the user picked one on the unified Partner tab, the partner query got the value as-is (no rows match) and the student query got it as-is (no rows match in the partner's surface). Silent empty. Fixed: route now adds the partner equivalent to a `partnerStatusFilter: string[]` when the user picks `'Under Review'`, and `fetchPartnerApplications` uses `.in('status', [...])` when the array has >1 element. The cross-taxonomy mapping is centralized in one place. **(4) Unified list had no at-a-glance stat cards** (the partner page has 4). Extended `GET /api/admin/applications/counts` to also return per-status + per-priority breakdowns (capped at 5000 rows per surface — surfaces a tiny `5000+?` amber hint when the pipeline exceeds that). Headline numbers apply the cross-taxonomy mapping once: `inReview = student 'Under Review' + partner 'In Review'`, `urgent = priority 'Urgent' or 'High'`. 4 stat cards now render below the header (Total in tab / Awaiting Review / In Review / Urgent), mirroring the partner pattern. New response shape: `{ total, online, partner, partnerCrm, offline, submitted, inReview, urgent, byStatus, byPriority, perStatusCapped }` (backward-compatible — old callers reading `total` etc. still work). **(5) Bulk result dialog was hard to scan when many rows failed**: a cross-taxonomy bulk-status move that fails 50 rows of 50 produced a 50-row wall of identical errors. Added a `list | grouped` toggle (errors bucketed by message + count + 3 sample ids) plus a `Copy IDs` button for triage in external tools (spreadsheet, chat). Both gated on `failed.length > 0`. **Bonus**: filtered-empty state on the unified list now has a one-click `Reset filters` button so a too-narrow search doesn't strand the admin. Disabled when no filter is active so it doubles as a hint about the current state. **Gates**: `pnpm ts-check` 0, `pnpm test` 14/14 124/124 passing, `pnpm lint:build` 0, `pnpm build` success. **i18n**: admin pages still en-only (per the deferred roadmap item).
- **Phase 33** (2026-06-13): Fold Partner Pipeline into unified Applications list. The standalone `/admin/partner-applications` list page (432 lines, basic table, no bulk, no export, no stat cards) was a strictly worse view of the same data that `/admin/applications` already shows (S28 unified list) — both queried the same `partner_applications` table, both showed the same columns, and the admin had to mentally merge the two when triaging a partner-pipeline review. **Folding**: `/admin/partner-applications/page.tsx` is now a 0-RTT server `redirect('/admin/applications?surface=partner')`. The unified list reads `?surface=` on mount and enters partner-only mode: page title flips to "Partner Pipeline" with a `← All Applications` escape link; source tabs hide (every row is "Partner CRM" by definition); 4 partner-specific columns surface (Partner org with company name from the joined `partners!partner_id`, color-coded Priority, Decision, Application #); "Add Application" + CSV export + stat cards + bulk ops + status/priority filters + search + sort all carry through unchanged; the unified bulk route's surface-aware routing already handled partner rows (S31) and Phase 32's cross-taxonomy status filter already translated `'Under Review' → 'In Review'`. **Sidebar**: removed the "Partner Pipeline" nav item (a stale link would just re-redirect). **Detail page back-links**: `/admin/partner-applications/[id]` (unchanged) now points back to `/admin/applications?surface=partner` so the user doesn't bounce through a redirect hop. **API**: `fetchPartnerApplications` now joins `partner:partners!partner_id (id, company_name, user_id)` so the "Partner" column can show the company name; `mapPartnerAppForUnifiedList` normalizes the Supabase 1:1 join (object OR single-element array — SDK contract has shifted between versions) and passes `priority / decision / application_number / partnerOrgId / partnerOrgName` through to the unified row shape. The `UnifiedAppRow` interface gained 4 optional partner-only fields; student rows return `null` for them and the page renders a `—` placeholder only if you somehow mixed them. **Why this is its own phase and not just folded into Phase 32**: the redirect + sidebar cleanup is a real UX change, not a bug fix. The admin would have noticed the missing nav item. **Gates**: `pnpm ts-check` 0, `pnpm test` 14/14 124/124 passing, `pnpm lint:build` 0, `pnpm build` success. **i18n**: admin pages still en-only (deferred per the roadmap).
- **Backfill — Phase 2.5** (admin emails, undocumented until now): `src/app/admin/emails/page.tsx` is a 584-line 3-column email template editor (list / editor / live preview). Self-tags "Phase 2.5". Templates moved from hardcoded TS to the `email_templates` table during this same phase, with category splits `drip | status | oneoff`. "Send test" button renders + sends the selected template to `ADMIN_EMAIL` so the admin can sanity-check before publishing. Production sends happen via the lead-detail "Send email" button (Phase 2.6). The drip scheduler (`src/lib/email/drip/scheduler.ts`) was rewritten around the same time so `scheduleDripSequence` reads from the DB instead of a code constant — admins can edit drip copy without redeploying. No DB migration was needed; the canonical migration already covers `email_templates`.
- **Backfill — S34** (admin cohorts view, undocumented until now): `src/app/admin/cohorts/page.tsx` is a 293-line read-only admin dashboard that groups applications by intake period. Self-tags "S34". Pairs with `/api/admin/cohorts`. Renders a card per cohort bucket (sorted by intake date) with: total applications, student count, partner org count if any, top programs at that intake, and a deep-link into the unified `/admin/applications` list pre-filtered for that intake. Used by admins during intake crunch (twice a year) to scan volume and pivot quickly. No new tables; reads from the existing `student_applications.intake` + `partner_applications.intake` columns.
- **Phase 34** (2026-07-04): Production hardening — Sentry + `/api/health` + the canonical pattern for cron / backup / DNS. Four real gaps before this phase: (a) zero error monitoring — a 500 at 3am was invisible to the solo dev; (b) zero uptime probe — Railway's health-check fell back to "process listening on PORT" which doesn't catch a stale Prisma client or a wedged scheduler; (c) AGENTS.md had a 3-week-pause backfill gap (Phase 2.5 + S34 existed in code but were never logged); (d) no canonical doc telling future-him where to look when something breaks at 3am. **(1) Sentry (`@sentry/nextjs@10.63.0`)**: env-gated on `SENTRY_DSN`. Empty DSN = no SDK init (browser), no `instrumentation.ts` runtime hook (server + edge), no `withSentryConfig` build wrapping, no source-map plugin. Set DSN → rebuild → errors + 10% of transactions show up in Sentry. Free tier (5K errors / 10K transactions) is comfortable for SICA's volume. `src/instrumentation.ts` splits via `NEXT_RUNTIME === 'nodejs' | 'edge'` and lazy-imports the right config. `src/sentry.server.config.ts` + `src/sentry.edge.config.ts` + `src/instrumentation-client.ts` all gate on the same DSN. `beforeSendTransaction` drops `/api/health` so the upmon doesn't burn quota. `sendDefaultPii: false`. Browser `tracesSampleRate: 0` (errors only) — flip to 0.1 when perf budget allows. **(2) `/api/health`** (`src/app/api/health/route.ts`, ~85 lines): GET, no auth, returns `{ status: 'ok' | 'degraded', timestamp, uptimeSeconds, version (git SHA), checks: { db: 'ok'|'down'|'skipped'|'timeout', env: { SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, RESEND_API_KEY, SENTRY_DSN, NEWS_CRON_SECRET, DRIP_CRON_SECRET } } }`. DB probe = `select('id', {count:'exact',head:true}).limit(1)` against `partner_applications` (smallest always-present + RLS-permissive for service role), bounded by a 2s race timeout. `'skipped'` = unset Supabase env, treated as healthy (don't page on half-set env). 200 ok / 503 degraded. The `checks.env` block surfaces which cron secrets are set so the uptime-monitor dashboard can confirm production auth wiring without grepping env. **(3) Custom server error capture**: `src/server.ts` had a try/catch around `handle(req, res)` and a `server.once('error')` listener that both `console.error`'d and bailed. Both now lazily `import('@sentry/nextjs').captureException(err, { tags: { source: 'custom-server' | 'custom-server-listener', url } })` when `SENTRY_DSN` is set; the listener also `await Sentry.flush(2000)` before `process.exit(1)` so the event ships. Best-effort — Sentry failures never block the error response or the exit. **(4) A new "Production / observability" section in AGENTS.md** consolidates the canonical pattern: where `/api/health` lives, how to enable Sentry, what to schedule when (the two cron endpoints + their secrets), email-deliverability DNS checks (SPF / DKIM / DMARC for `sica.com.cn` before going live), and Supabase backup policy (PITR daily by default on paid plans, manual restore from dashboard). **(5) AGENTS.md backfill**: Phase 2.5 (admin emails, ~2026-06-05) + S34 (admin cohorts, ~2026-06-08) entries restored above so the changelog reflects what the codebase actually contains. **Gates**: `pnpm ts-check` 0, `pnpm test` 14/14 124/124 passing, `pnpm lint:build` 0, `pnpm build` succeeds. **Verification**: dev server `GET /api/health` returns `{ status: 'ok', db: 'skipped', env: { SUPABASE_URL: false, SENTRY_DSN: false, ... } }` (env empty locally → all env checks false, db skipped, healthy); with full `.env` the env switches flip to true. Setting `SENTRY_DSN` makes the build pick up `withSentryConfig` (verified via `pnpm build` log mentioning the Sentry plugin); unsetting it leaves the build silent.
- **Phase 35** (2026-07-04): Bulk AI university generator on `/admin/universities`. The existing `AIGenerateModal` was single-row — admin typed one name, AI filled 30 fields, admin saved. No way to seed 5+ new universities at once without 5× clicks. New **"Bulk Generate (5)"** button (deep-blue outline variant, sits left of the single-row AI Generate) opens `AIBulkGenerateModal` — a 6-step wizard: **idle → suggesting → review-names → generating → review-details → saving → done**. **(1)** New API endpoint `POST /api/admin/universities/bulk-suggest-names` (admin-gated via `requireAdmin`): reads first 500 existing universities from `universities` by ranking (only `name / name_cn / slug / city / intl_students` columns to keep the user-message small), embeds them in the prompt as "DO NOT repeat any of these names or slugs", and asks the LLM for exactly 5 NEW entries that "actively accept international students — public universities with English-taught programs + scholarship tracks". Server-side dedup: rejects AI suggestions whose name (case-insensitive) or slug matches anything in the existing catalog; rejects intra-batch duplicates; defends against markdown-fence + trailing-comma LLM fail-modes; validates `ranking` is a finite positive integer; caps at 7 server-side. Returns `{ suggestions: [{name, nameCn, slug, city, cityCn, ranking}] }` — lightweight payload only, detail generation is a separate phase. **(2)** Detail phase: reuses the existing `POST /api/ai/generate-university` route, called **in parallel** from the modal (one fetch per checked suggestion). Each call streams SSE; the modal collects `data: {parsed: {...}}` events from each stream as they arrive (server-side validated JSON becomes the canonical payload, mirroring the single-modal pattern). Per-row progress (`pending → generating → ready/error`); total wall time ≈ the slowest call (~30s), not 5× the call time. Cancel button aborts all in-flight `AbortController`s. **(3)** Save phase: parallel POSTs to `POST /api/universities`; per-row result classified as `saved | duplicate | error` (duplicate detected via `/duplicate|unique|exists/i` regex on the error message — DB unique constraint on `slug` surfaces 400s with that wording). Final done screen shows counts: "3 saved · 1 duplicate skipped · 0 failed · 1 still pending retry via single-row AI Generate". **(4)** Wiring: `/admin/universities/page.tsx` grew 12 lines (new `showBulkModal` state + a shared `refreshUniversities()` callback used by both modals so list-refresh logic isn't duplicated). **(5)** Bulk button styled deep-blue (`border-[#1B2A4A]`) to distinguish from the existing crimson AI Generate + Add University. **Why two prompts (suggest + detail) vs one big call**: the cheap pass (~10s, 1.5K tokens) gives the admin a checkpoint to prune 1-2 bad names before we burn the long-tail tokens of 5× detail generations (~4K tokens each). Saves the admin ~50% of cost + ~70% of wall time when they reject even one suggestion. **Gates**: `pnpm ts-check` 0, `pnpm test` 14/14 124/124 passing (regression-free), `pnpm lint:build` 0, `pnpm build` succeeds (route table shows `ƒ /api/admin/universities/bulk-suggest-names` + bulk modal bundled into `/admin/universities`). **Live verification path** (when env has API key): admin clicks Bulk Generate → Suggest → sees 5 cards within ~10s with new school names that don't overlap with their catalog → unchecks 1 → Continue (4) → ~30s of parallel generation → review detail cards → Save 4 → 4 POSTs return 201, list refreshes. Duplicates on slug surface as the existing 400 message from the unique constraint and the modal classifies + reports them without blocking the other 3.
- **Phase 36** (2026-07-04): Apply Phase-34-style error capture to every AI call site + per-admin rate limiting on the 4 admin AI endpoints. Three real problems Phase-34-Sentry-rollout exposed: (a) when an admin's AI generation fails the toast shows the error, but no one else knows — zero signal in Sentry, all errors lost; (b) `/api/ai/chat` failures throw a generic "Failed to send message" to the end-user with no visibility into what the provider actually returned; (c) the four admin AI routes (`generate-university`, `generate-blog`, `bulk-suggest-names`, `news/automation/run`) all share one provider quota and **none had admin auth + none had rate limits** — a determined admin could `for i in {1..1000}; do fetch(...); done` and burn the monthly token allowance in a single bash loop. Phase 1-era code path, never caught. **(1) New shared helper** `src/lib/ai/with-capture.ts` (~75 lines): `captureAIError(route, err, extra?)` that lazy-imports `@sentry/nextjs`, env-gates on `SENTRY_DSN` (no-op when unset, matches Phase 34 / Phase 29 patterns), tags every event with `source: 'ai-route'`, `route`, `aiProvider: ${process.env.AI_PROVIDER}`, and forwards arbitrary `extra` context (stage, universityName, responseLength, etc.) into Sentry's `extra` field for dashboard filtering. Never throws — fail-safe wrapper. **(2) New shared helper** `src/lib/ai/admin-ai-rate-limit.ts` (~135 lines): two rate-limiters reusing `checkRateLimit` from Phase 14. `checkAdminAIRateLimit(userId, action)` — keyed by `auth.user.id`, central limit table `ADMIN_AI_LIMITS = { 'generate-university': 10, 'bulk-suggest-names': 5, 'generate-blog': 3, 'run-automation': 5 }`, all 15-minute windows. Returns `{ blocked: false, remaining } | { blocked: true, response }` where the response is a fully-formed NextResponse with status 429 + `Retry-After` header + `{ error, code: 'AI_RATE_LIMITED', retryAfterSec }` body. `checkChatbotRateLimit(request)` — keyed by IP from `x-forwarded-for` (first hop, RFC 7239 standard) → `x-real-ip` → `'unknown'` shared bucket. Limit 10/15min. Returns 429 + `code: 'CHATBOT_RATE_LIMITED'`. **(3) Auth fix (related)** — the 4 admin AI routes had no `requireAdmin` / `getRequestAuth` check; added Bearer-token validation to `generate-university`, `generate-blog`, and `news/automation/run` so per-admin rate-limiting can key on `user.id`. `bulk-suggest-names` already had `requireAdmin` from Phase 35. The two admin modals (`ai-generate-modal.tsx`, `ai-bulk-generate-modal.tsx`) migrated from raw `fetch()` to `apiFetch()` so they actually send the Bearer token (apiFetch attaches it from `supabase.auth.getSession()` automatically). **(4) Per-call-site capture**: 6 catch sites now call `captureAIError(...)` with the right `stage` (request | stream | parse | parse-or-dedup | topic-attempt | run): `ai/chat` outer + streamProviderResponse inner; `ai/generate-university` outer + stream + parse; `ai/generate-blog` outer + stream + parse; `admin/universities/bulk-suggest-names` parse-or-dedup + request; `admin/news/automation/run` whole-run; `news-automation-runner` per-topic-attempt. The chatbot-client error UX also fixed — `ChatWindow.tsx` now parses the server's JSON `{ error }` body (was `throw new Error('Failed to send message')` with no context) so end-users see "Too many messages. Wait 30s before trying again." on 429 and the AI provider's actual error text on 500 instead of a generic apology. **(5) Unit tests**: new `src/lib/__tests__/with-capture.test.ts` (4 tests — env-gated no-op when DSN unset, accepts Error/string/object/null/undefined, accepts arbitrary extra, no-throw when DSN set); new `src/lib/__tests__/admin-ai-rate-limit.test.ts` (8 tests — per-action limits, key isolation per (user,action), chatbot per-IP isolation, x-forwarded-for first-hop extraction, x-real-ip fallback, headerless "unknown" bucket). **Gates**: `pnpm ts-check` 0, `pnpm test` 16/16 files 136/136 passing (was 14/14 124/124 — +2 new files +12 new tests), `pnpm lint:build` 0, `pnpm build` success (route table confirms all 4 admin AI + 1 public AI + the 2 cron variants are still present). **Risks closed**: the unauthenticated AI endpoints from Phase 1 are now admin-gated; provider quota burn requires an admin login token + per-admin cap; an empty/malformed AI response surfaces in Sentry's dashboard within seconds (free tier lag ~30s) instead of disappearing into the void.
- **Phase 37** (2026-07-04): Admin Portal i18n sweep — translates 4 admin surfaces + 2 admin-only modals from en-only to en + zh (matching the S43a student / S43b partner portal convention from back in 2026-06-06). L11 + L2-deferred from the original "S5+ 计划" backlog finally cleared. **(1)** New namespaces in `src/lib/i18n-translations.ts`: `adminNav.*` (20 keys — sidebar items + sign-out + loading + administrator label + brand badge), `adminAuth.*` (24 keys — login/register forms + invite-only gating messages), `adminDashboard.*` (33 keys — welcome greeting with `{{name}}` interpolation, 4 stat cards with `{{n}}` / `{{active}}` / `{{unlinked}}` interpolation, recent-applications table columns, lead-pipeline card, quick-actions list, recent-activity card, error banner), `adminUniversities.*` (76 keys covering list + bulk-generate + AI-single-modal + AI-bulk-modal — title + subtitle + 3 button labels + 7 table columns + delete confirm + 6 toast strings + bulk-save pluralization + the entire 5-step bulk modal flow + the AI-single modal titles/body/labels/buttons/errors). All keys have both en + zh entries; the existing `src/lib/i18n-translations.test.ts` drift test passed confirming parity. The translation file grew 3735 → 4103 lines (+368 lines, 166 new en + 166 new zh + ~36 line keys). **(2)** Page refactors: `src/app/admin/layout.tsx` — sidebar nav switched from inline `label`/`labelCn` pairs (a half-done pattern) to `key`-based `t(\`adminNav.${item.key}\`)` so adding a sidebar item means one key per locale + one entry, not string duplication in two languages; the loading + redirect + signOut + administrator text uses `t()`. `src/app/admin/login/page.tsx` (143 LoC) + `src/app/admin/register/page.tsx` (243 LoC) — every label/placeholder/button/error/heading routes through `t(adminAuth.*)`; the register form's invite-only gating screen shows the translated "checking invite…" / "invite required" copy. `src/app/admin/dashboard/page.tsx` (451 LoC) — welcome message interpolates user name, stat cards get `{{n}}`-aware subtitles, recent-applications table headers + empty state + add-first CTA are all `t()`, lead pipeline card's 3-channel breakdown + unassigned/needs-followup counters, quick-actions list, recent-activity empty state, error banner with retry button. `src/app/admin/universities/page.tsx` (294 LoC after refactor) — title + subtitle + 3 button labels + search placeholder + 7 table columns + view-regenerate-edit-delete tooltips + showing-N-of-M footer + delete confirm dialog + both AI modal toast strings + pluralized "saved N universities" toast. **(3)** Admin-only modals translated in place — `src/components/admin/ai-generate-modal.tsx`: idle/generating/success/error state titles + bodies + button labels + parse-failure error + progress + retry/close buttons (~20 t() refs). `src/components/admin/ai-bulk-generate-modal.tsx`: step-indicator labels (5 steps), modal title + intro + step-explainer copy + review-instructions + generating-progress (with `{{done}}/{{total}}` interpolation) + cancel/restart/save/done buttons + per-row status labels (Pending/Generating/Ready/Error/Cancelled) + the 8-field data-summary grid in the review detail step + save/saving/done states with the `{{saved}}/{{duplicate}}/{{failed}}` triple counters (~30 t() refs). The step indicator switched from inline `label: string` array to `labelKey: string` so step labels are translation-driven instead of baked-in. **Out of scope (intentional)**: `/admin/students`, `/admin/applications`, `/admin/programs`, `/admin/scholarships`, `/admin/partners`, `/admin/leads`, `/admin/documents` (only the list/detail pair is done — Phase 2), `/admin/news`, `/admin/emails`, `/admin/cohorts`, `/admin/assessments`, `/admin/fees`, `/admin/settings`, `/admin/universities/new` (548-line form), `/admin/universities/[slug]/edit` (re-export of new/page), every `/admin/applications/[id]`, `/admin/leads/[id]`, etc. detail page. **DB enum values stay untranslated** (status, document category, application priority, degree level) to keep the DB ↔ UI contract stable. **Gates**: `pnpm ts-check` 0, `pnpm test` 16/16 files 136/136 passing (the 5 i18n drift tests still pass with all 166 new keys), `pnpm lint:build` 0, `pnpm build` success. **Live verification path**: admin sets `sica-locale=zh` cookie or picks 中文 from the language picker → `/admin/dashboard`, `/admin/universities`, `/admin/login`, `/admin/register`, the sidebar nav, and both AI modals render Chinese; falls back to en when the key is missing (current en block has them all). The i18n-sweep scope (~370 lines of translation + ~250 lines of refactor across 7 files) is meaningful but doesn't reach 100% admin coverage — deferred to follow-up phases for the remaining 14 pages.
- **Phase 38** (2026-07-04): Lead data quality — three small fixes for the chat / leads / bulk-AI flow that all surfaced from real admin use this week. **(1) Admin leads list + detail fall back to email local-part when `name` is null.** User reported "leads not showing names" — root cause was 9 `chat_leads` rows (the user's only "new" leads, including `nayelievega140@gmail.com` from 2026-07-03, all of them with `name: null`). The chat widget's "Save your progress" form has Name as optional, Email as required — every visitor who only typed email landed as NULL. The fix doesn't change the DB; it makes the admin UI show `nayelievega140` instead of `(no name)`. `src/app/admin/leads/page.tsx` (line 595) + `src/app/admin/leads/[id]/page.tsx` (line 333): `{lead.name || (lead.email ? lead.email.split('@')[0] : '(no name)')}`. Contact-form and assessment-form leads already had names, so they're unaffected. **(2) Chat widget pre-fills Name + Email from the conversation.** New helpers `extractNameFromMessages()` + `extractEmailFromMessages()` (exported from `src/components/ai/ChatWindow.tsx`) scan user messages for `my name is X` / `I'm X` / `this is X` / `call me X` / `calls me X` + any email-looking string, latest-message-first. 13-word stop-list filters "I am looking for…", "I am interested in…", "I am a student…" false positives. `LeadPanel` accepts `messages` prop, runs the extractors in `useMemo`, and applies them in a `useEffect` that only fires when `panel === 'open'` AND the field is currently empty — never overwrites what the visitor typed, re-runs on every new message so a late introduction still surfaces. "✓ from chat" gold pill renders next to Name + Email only while `data.name === detectedName` / `data.email === detectedEmail` (vanishes the moment they edit). 18 vitest cases in `src/components/ai/__tests__/ChatWindow.test.ts` covering canonical phrasings, false-positive filtering, multi-introduction recency, length sanity bound, and the email extractor. **API signatures loosened** to `ReadonlyArray<Pick<ChatMessage, 'role' | 'content'>>` so tests don't have to fake `id` fields the extractors don't read. **(3) Bulk AI university generator — `handleSuggest` swapped from raw `fetch()` to `apiFetch()`.** Phase 36 fixed `handleGenerateDetails` (the detail-phase call) but missed this one — every admin's first click of "Suggest 5" was 401-ing with "Not authenticated" because `requireAdmin` was rejecting the missing Bearer token. After that 401 the rest of the modal was unreachable. Comment added pointing at Phase 36 for grep-ability. **Gates**: `pnpm ts-check` 0 / `pnpm lint:build` 0 / `pnpm test` 17/17 files 154/154 passing (was 136/136, +18 new). **Live-verify**: open chat widget on any public page, type "Hi I'm Maria from Nigeria", click "Save my progress" → Name auto-fills with `Maria` + gold "✓ from chat" pill; type "actually my email is maria@example.com" → Email auto-fills + pill appears. Re-open `/admin/leads`, the existing 9 NULL-name chat_leads now show `nayelievega140` / `yvesniyonkunda` / etc. Re-open `/admin/universities`, click "Bulk Generate (5)" → no more 401.
- **Phase 39** (2026-07-08): 3 SEO listicle pages — `/mbbs-in-china`, `/cheapest-universities-in-china-for-international-students`, `/best-universities-in-china`. Self-contained client components with shared `<ListicleLayout>` shell, JSON-LD `ItemList` + `FAQPage` schema, social-meta (`og:image` covers with branded SVG per page), `next-sitemap` discoverable from `/`. New i18n keys: `listicle.*` (en + zh). **Gates**: ts-check 0, test 17/17 154/154, build success. Live: 3 URLs return 200.
- **Phase 39b** (2026-07-08): refactor the 3 Phase-39 pages to use a shared `<GuidePage>` component (`src/components/listicle/GuidePage.tsx`) — single source of truth for hero, intro, FAQ, related-articles, JSON-LD, social-meta. **Gates**: ts-check 0, test 17/17, build success.
- **Phase 40** (2026-07-08): 5 more SEO listicle pages reusing `<GuidePage>` — `/mba-in-china`, `/top-engineering-universities-china`, `/study-in-china/[city]` × 6 (beijing/shanghai/guangzhou/hangzhou/wuhan/chengdu), `/study-in-china-vs-russia-for-mbbs`, `/cost-of-studying-in-china`. All schema-tagged, all discoverable from sitemap. **Gates**: ts-check 0, test 17/17, build success.
- **Phase 41** (2026-07-08): 5 SEO listicle pages + guides hub overhaul — `/scholarships-for/[country]` × ~36 generated paths, `/scholarships/[slug]/eligible-countries` deep-link page, `/study-in-china` pillar hub. **Gates**: ts-check 0, test 17/17, build success. Sitemap includes all new URLs.
- **Phase 41b** (2026-07-08): `VideoTestimonials` component (client island, Play-poster + YouTube iframe on click), nav icon polish (testimonials icon swap), QR-code `<Image>` optimization to AVIF/WebP via `next/image`. **Gates**: ts-check 0, test 17/17, build success.
- **hotfix** (2026-07-08): include the two new VideoTestimonials review videos (mp4 files in `public/videos/`) in the deploy — the assets were referenced by `<VideoTestimonials>` but not committed yet. Added `public/videos/*.mp4` to the deploy.
- **S42** (2026-07-08): header menu polish — active-route highlight on the current page, anchor-link for the `#why-china` section on home, flatten the Programs submenu to a single link (instead of 5 degree-level subitems that nobody clicked). **Gates**: ts-check 0, test 17/17, build success.
- **S42** (2026-07-08): header i18n cleanup — added 4 missing en + zh keys for "Portal Login" + the new Admissions contact header link. **Gates**: ts-check 0, test 17/17 (5 i18n drift tests), build success.
- **Phase 1 funnel fixes** (2026-07-09): broken links, dead buttons, email canonicalization, name pass-through. (1) `/contact` and `/assessment` post-submit redirect — the router was calling `push('/thank-you?...')` but the wrong params were passed; canonicalized. (2) `lead.email` lowercase in `/api/leads` POST so duplicate-detection matches across case-variants (`@Gmail.com` ↔ `@gmail.com`). (3) LeadPanel name pass-through — the chat widget's collected `data.name` was being shadowed by an empty `<input defaultValue>`; fixed by binding `value` + `onChange`. (4) Footer WhatsApp link used the old phone number — updated to current. **Gates**: ts-check 0, test 17/17 154/154, build success.
- **Phase 2 (commit 1)** (2026-07-09): `/resources` page — the new self-serve landing for Whop-shoppers. Renders the template catalog, study plans, community invite, FAQ. Header nav + footer both surface it as a primary entry. i18n keys: `resources.*` (en + zh). 5 new components under `src/components/resources/`, 1 page, 1 nav update, 1 footer update. **Gates**: ts-check 0, test 17/17, build success.
- **Phase 2 (commit 2)** (2026-07-09): 2-product path selector + DIY escape hatches. The SICA site now sells two products side-by-side: full-service (SICA handles it) and self-serve (DIY with templates + study plans + community via Whop). 3 surfaces: (1) home hero — 2-pill selector above the headline, full-service left (crimson hover), self-serve right (white hover), no "choose your path" gate; new i18n keys `choosePath.path{1,2}Label` (en + zh). (2) University detail right rail — SICA Support Card now has full-service primary button + deep-blue outline "DIY →" secondary, GA-tracked. (3) Program detail Support Card — same primary+secondary pattern, `/resources?program=<slug>`. (4) StickyApplyBar — adds a low-affordance text-link "DIY →" between WhatsApp and Apply so it doesn't compete with the primary CTA. Same intent at 3 entry points. **Gates**: ts-check 0, lint:build clean, test 17/17 154/154, build success.

- **公开 admin 注册**: `/admin/register` 仍开放 (需 `ADMIN_INVITE_TOKEN` env)

## S1-S3 重要决策记录
- **S1** (2026-06-02): Bearer-token auth 模式, 不是 cookie-based. Supabase service-role client 无法 `getUser()`, 必须用 anon key + per-request JWT
- **S2** (2026-06-02): 删 `storage-server/` + 7 个 dead deps (-97MB). Admin 注册用 `ADMIN_INVITE_TOKEN` env 门控. Partner login 改真实 Supabase auth
- **S3** (2026-06-02): 4 个公共页全转 RSC (home/about/contact/assessment), 4 个 filter 页去掉 `data.ts` 完整数组 import 改 on-mount fetch. i18n 重构: 翻译表提到 server-safe 文件 + cookie locale 同步
- **Migrate** (2026-06-02): 从 Volcengine 迁到官方 supabase.com. RLS 出现无限递归, 改用 `SECURITY DEFINER public.is_admin()` 函数解决
